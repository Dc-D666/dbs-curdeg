# 本地开发环境变量（复制为 backend/.env，永不提交；提交只允许 .env.example）
# 本地开发连 docker-compose.dev.yml 起的 MySQL/Redis（本机起容器），
# 或经 SSH 隧道连服务器宝塔 MySQL 5.7.44 guild 库（本地 .env 按需覆盖）。
# 生产（docker compose）下不设 MYSQL_HOST，config.py 默认连 compose 服务名 mysql
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=guild
MYSQL_USER=guild
MYSQL_PASSWORD=change_me

REDIS_HOST=localhost
REDIS_PORT=6379

JWT_SECRET=dev-only-secret
SMTP_USER=3303188265@qq.com
SMTP_PASSWORD=change_me_smtp_auth_code
SMTP_FROM=3303188265@qq.com

ZHIPU_API_KEY=change_me
DEEPSEEK_API_KEY=change_me

DEBUG=true
