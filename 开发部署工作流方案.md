# 开发 → 部署一体化工作流方案（仿腾讯频道课设 · FastAPI 技术栈）

> 适用：Python 3.12 FastAPI + MySQL 8 + Redis 7 + Vue3(Vite) + Kotlin Compose Android
> 服务器：新租 4C4G（腾讯云轻量，OpenCloudOS/Ubuntu 均可）
> 目标：本地 `git push` 一条命令，服务器自动完成更新部署，消灭手动 SSH 操作
> 前置：已注册域名一个（腾讯云 DNSPod 解析）

---

## 一、一图看懂新工作流（对比你现在的方式）

```
【你现在的方式 —— 每次迭代要 5 步手动】
本地改 → 本地测 → git push → SSH 上服务器 git pull → 手动 build + 重启    ✗ 繁琐易错

【新方式 —— 开发期与上线期各就位】
开发期：
  本地 → docker compose up（MySQL/Redis 依赖一起起）→ 改代码热更新 → 本地测试
上线期（git push 触发，全自动）：
  git push → GitHub Actions 云端自动构建 → 推到镜像仓库 → SSH 通知服务器
          → 服务器 docker compose pull + up -d    （10 秒，零手动）
```

**核心思想**：构建放云端（不吃服务器资源），服务器只做「拉镜像 + 换容器」；`docker compose` 保证本地和服务器环境完全一致（"在我机器上是好的"问题消失）。

---

## 二、仓库与项目结构（Monorepo 单仓库）

```
project-root/                        # GitHub 仓库（与服务器 /opt/<project> 同名）
├── backend/                         # FastAPI
│   ├── app/
│   │   ├── main.py                  # 入口
│   │   ├── api/                     # 路由 auth/community/content/interact/ai/notification
│   │   ├── models/                  # SQLAlchemy 模型
│   │   ├── schemas/                 # Pydantic
│   │   ├── core/                    # config/security
│   │   ├── ai/                      # llm_gateway / 审核 / rag / 摘要
│   │   ├── ai_worker.py             # 独立 AI worker 进程入口
│   │   └── ws/                      # WebSocket 通知
│   ├── tests/
│   ├── alembic/                     # 数据库迁移
│   ├── requirements.txt
│   ├── Dockerfile
│   └── .env.example
├── frontend/                        # Vue3 + Vite
│   ├── src/
│   ├── Dockerfile                   # node 构建 → nginx 产物
│   └── .env.example                 # VITE_API_BASE 等
├── android/                         # Kotlin Compose（APK 单独发，不进 Docker）
├── deploy/
│   ├── docker-compose.yml           # 生产编排
│   ├── nginx/nginx.conf             # 反向代理 + 静态 + HTTPS
│   ├── deploy.sh                    # 服务器一键更新脚本
│   └── .env.example                 # 生产环境变量模板
└── .github/workflows/deploy.yml     # CI/CD
```

> Android App 不走 Docker/CI：本地 Android Studio 打包 APK，连的是同一个 `api.你的域名.com`，API 契约一致（OpenAPI 文档可生成 SDK）。

---

## 三、Git 工作流（课设够用，别复杂化）

- **只用一个 `main` 分支**即可，每次提交都保持可运行（因为 main 一 push 就自动部署）。
- 提交规范：`feat: 发帖功能` / `fix: 评论楼中楼 bug` / `docs: 接口文档`。
- `.gitignore` 必须包含：`backend/.env`、`frontend/.env`、`deploy/.env`、`__pycache__/`、`node_modules/`、`dist/`（密钥永不入库）。

---

## 四、本地开发环境（第一天就配好）

1. 装 Docker Desktop（Windows）。
2. 仓库根目录放一份**开发用** `docker-compose.dev.yml`，只起依赖：

```yaml
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: dev123456
      MYSQL_DATABASE: channel
    ports: ["3306:3306"]
    volumes: [mysql_data:/var/lib/mysql]
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
volumes:
  mysql_data:
```

3. 后端本地跑：`uvicorn app.main:app --reload`（热更新）；前端 `npm run dev`（Vite 热更新）。
4. 迁移：`alembic upgrade head`。**以后所有库表变更都写 migration，不用手改生产库。**

---

## 五、Dockerfile 设计

### `backend/Dockerfile`（Python slim，镜像约 300MB）
```dockerfile
FROM python:3.12-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends \
    default-libmysqlclient-dev gcc && rm -rf /var/lib/apt/lists/*
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app ./app
COPY alembic ./alembic
COPY alembic.ini .
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
# AI worker 用同一镜像：docker compose 里 command 覆盖为 python -m app.ai_worker
```

### `frontend/Dockerfile`（node 构建 → nginx 产物，多阶段）
```dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx-web.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

> 前端产物最终由**最前面那台生产 nginx**（统一 443 入口）转发/托管，见下一节。

---

## 六、生产编排 `deploy/docker-compose.yml`

```yaml
services:
  api:
    image: registry.cn-hangzhou.aliyuncs.com/<你的namespace>/channel-api:latest
    restart: always
    env_file: .env
    depends_on: [mysql, redis]
    volumes: [uploads:/app/uploads]
    networks: [app]
  ai-worker:                     # 同一镜像，不同 command
    image: registry.cn-hangzhou.aliyuncs.com/<你的namespace>/channel-api:latest
    command: ["python", "-m", "app.ai_worker"]
    restart: always
    env_file: .env
    depends_on: [mysql, redis]
    networks: [app]
  mysql:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: channel
    volumes: [mysql_data:/var/lib/mysql]
    command: ["mysqld", "--innodb-buffer-pool-size=384M", "--max-connections=120"]
    networks: [app]
  redis:
    image: redis:7-alpine
    restart: always
    volumes: [redis_data:/data]
    networks: [app]
  nginx:                         # 唯一对外入口 80/443
    image: nginx:alpine
    restart: always
    ports: ["80:80", "443:443"]
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/cert:/etc/nginx/cert:ro
      - uploads:/data/uploads:ro
    depends_on: [api]
    networks: [app]

networks: { app: {} }
volumes: { mysql_data: {}, redis_data: {}, uploads: {} }
```

**nginx 职责**（`nginx/nginx.conf`）：
- 443 强制 HTTPS（证书挂 `./nginx/cert`）；
- `api.你的域名.com` → 反代到 `api:8000`，`/ws` 路径配 WebSocket 升级头；
- `你的域名.com` / `www.` → 静态托管前端产物（前端镜像 dist 挂载，或单独 web 服务，见注）；
- `/uploads/` → 指向 `uploads` 卷，实现「用户上传文件直接由 nginx 服务」。

> 注：若嫌前端单独起容器，也可 `nginx` 容器直接挂载前端 `dist` 卷。二选一，方案里用「nginx 统一入口 + dist 卷挂载」最省内存。4C4G 下两种都无压力。

---

## 七、CI/CD：GitHub Actions 全自动部署

### 方案 A（推荐，标准）：云端构建 → 镜像仓库 → 服务器拉取
需要：① GitHub 仓库（免费）② 阿里云容器镜像服务个人版（免费，或 Docker Hub）③ 服务器上放好部署目录。

`.github/workflows/deploy.yml`：
```yaml
name: build-and-deploy
on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: pip install -r backend/requirements.txt
      - run: cd backend && pytest -q

  build-and-deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 登录镜像仓库
        run: echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login registry.cn-hangzhou.aliyuncs.com \
             -u "${{ secrets.REGISTRY_USERNAME }}" --password-stdin
      - name: 构建并推送 API 镜像
        run: |
          docker build -t registry.cn-hangzhou.aliyuncs.com/<ns>/channel-api:latest backend/
          docker push registry.cn-hangzhou.aliyuncs.com/<ns>/channel-api:latest
      - name: 构建并推送前端镜像
        run: |
          docker build -t registry.cn-hangzhou.aliyuncs.com/<ns>/channel-web:latest frontend/
          docker push registry.cn-hangzhou.aliyuncs.com/<ns>/channel-web:latest
      - name: SSH 到服务器执行更新
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /opt/channel && docker compose pull && docker compose up -d && docker image prune -f
```

GitHub 仓库 Secrets 里配 4 项：`SERVER_HOST`、`SERVER_SSH_KEY`、`REGISTRY_USERNAME`、`REGISTRY_PASSWORD`（都不入库、不公开）。

### 方案 B（简化，不想注册镜像仓库时用）
GitHub Actions 把 `backend/` 源码 + 前端 `dist/` 直接 `rsync` 到服务器，服务器执行 `docker compose up -d --build`。4C4G 本地构建扛得住。缺点：构建时间更长、镜像仓库管理缺失，但零第三方账号。

> 课设推荐 **方案 A**：答辩时"云端 CI + 容器化 + 一键回滚"叙事完整，且一劳永逸。

---

## 八、新服务器初始化（4C4G，一次性）

```bash
# 1. 基础 + Docker（腾讯云轻量 OpenCloudOS/Ubuntu 均可）
yum install -y git vim  # Ubuntu: apt install -y git vim
curl -fsSL https://get.docker.com | sh
systemctl enable --now docker
# docker compose 插件（若镜像没带）
# 2. 部署目录
mkdir -p /opt/channel/{nginx/cert,uploads}
cd /opt/channel
# 3. 放 docker-compose.yml、nginx/nginx.conf、.env（用 ssh_upload / 或从仓库拉取部署目录）
# 4. 首次启动
docker compose up -d
```

> 建议：`deploy/` 目录里的 compose/nginx/.env.example **也进 git 仓库**，服务器上只多一份本地 `.env`（密钥）+ 证书目录。这样部署配置也能随代码走、可追溯。

---

## 九、域名 + HTTPS（你已有域名）

| 子域名 | A 记录 → 指向 | 用途 |
|---|---|---|
| `你的域名.com` / `www.` | 新 4C4G 服务器 IP | Web 前端 |
| `api.你的域名.com` | 新 4C4G 服务器 IP | API（REST） |
| `ws.你的域名.com` | 新 4C4G 服务器 IP | WebSocket（可并入 api，也可独立） |
| `dev.你的域名.com` | （可选）第二台服务器 | 测试/预发环境 |
| 旧项目子域名 | 旧 2C2G 服务器 IP | greendoc 等照旧不动 |

- 在腾讯云 **DNSPod** 免费解析，A 记录指向各服务器公网 IP（或轻量自带防火墙开 80/443）。
- 证书：腾讯云 SSL 免费证书 90 天续期，或宝塔一键 / certbot（Let's Encrypt）。**一张泛域名证书 `*.你的域名.com` 可覆盖上面全部子域名**。
- App 端连 `https://api.你的域名.com` + `wss://ws.你的域名.com`。

---

## 十、更新 / 回滚 / 备份（发布后你关心的）

- **日常更新**：`git push` → 自动完成，无手动步骤。
- **回滚**：镜像全部带版本号 `channel-api:2025-06-01`（CI 里打日期 tag），服务器上 `docker compose` 指回旧 tag 即回滚；或 `git revert` 后再 push。
- **数据库**：只通过 `alembic` 迁移，禁止手改生产库。AI 审核/摘要等新增字段用可空列 + 向后兼容，避免回滚时炸库。
- **备份**：写一个 cron 每日 `mysqldump` + 上传对象存储；上传文件目录随 MySQL 一起备份。
- **健康检查**：nginx 加一个 `/healthz` 反代到 api 存活探针，浏览器直接看部署是否成功。

---

## 十一、落地节奏（贴合你还没开发的现状）

| 阶段 | 做什么 | 部署相关动作 |
|---|---|---|
| 现在（开发前） | 把本文档 + 方案定稿；去租 4C4G | —— |
| 第 1 周（脚手架） | `git init` + GitHub 仓库；建 monorepo 骨架；写两份 Dockerfile + compose + deploy.yml | 新服务器到手时跑「第八节」初始化；首次手动部署通一次 |
| 第 2~8 周（功能开发） | 按课设方案开发，日常只用 `docker compose.dev.yml` + 热更新 | 功能每合一段就 push 一次，顺便验证自动部署 |
| 第 9~10 周（上线） | 域名解析 + HTTPS + 看板 | 之后每次 `git push` 即上线，答辩前可一键回滚演示 |

> 关键：**部署骨架（Dockerfile/compose/CI）在开发第 1 周就搭好**，让"push 即部署"从第一个功能就生效，而不是最后临时补——这正是摆脱"每次手动部署很麻烦"的根本办法。

---

## 十二、待你确认 / 下一步

1. 新 4C4G 服务器租好后，把 IP/密码给我（或本机 dsh-ssh 配好），我帮你跑第八节初始化 + 首次部署。
2. GitHub 仓库名/镜像仓库 namespace 定了告诉我，我把 deploy.yml 里的占位符填实。
3. 开发脚手架（backend/frontend 目录 + Dockerfile + compose + CI）需要我现在就先搭起来吗？
