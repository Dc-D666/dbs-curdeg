# 前端 UX / 交互体验审查报告

**项目**: `D:\DBS-curdeg\frontend`（channel-web）
**框架**: Vue 3.4 + vue-router 4 + Pinia + TDesign Vue Next + Vite 5
**审查范围**: `src/` 全部路由、23 个视图、20+ 组件、stores/composables/utils
**审查方式**: 按 `frontend-ux-auditor` skill 静态代码走查（后端不可用，未做运行时截图验证，所有问题均为代码推断）
**日期**: 2026-08-31

---

## 路由树（全局导航结构）

```
带底部 Tabbar（4 个 tab 精确匹配 path）:
  / 首页    /discover 发现    /notifications 通知    /me 我的

全屏无 Tabbar（需页面自带返回）:
  /c/:id 频道详情      /c/:id/admin 频道管理后台
  /c/:id/boards/:bid/post/new 发帖/编辑帖
  /p/:id 帖子详情      /users/:id 用户主页
  /login /register     /me/* 个人子页   /dashboard 运营看板
  404 兜底页
```

## 模拟路径结论（P1–P4）

| 路径 | 结论 |
|---|---|
| P1 直接刷新深链 | `/c/:id`、`/p/:id` 可独立渲染 ✅；但桌面 ≥1024px 下 `/c/:id` 隐藏了含返回按钮的页头（问题 #6），游客无任何导航 |
| P2 列表→详情→返回 | 返回入口存在但目的地错误/单一（#13–#16）；全站无 `<keep-alive>`，返回后搜索状态、滚动位置、已加载分页全部丢失（#40） |
| P3 表单→提交 | 发帖有草稿+离开拦截 ✅，但有 3 处内容丢失路径（#2/#3/#4）；快评取消即丢稿（#2） |
| P4 失败路径 | 多处把「网络错误」误显示为「不存在」且无重试（#7/#10/#11/#19）；通知列表失败永久骨架屏（#7） |

---

## 🔴 高严重度（用户卡死 / 内容丢失）—— 10 项

| # | 页面/组件 | 问题类型 | 影响的用户场景 | 修复建议 |
|---|---|---|---|---|
| 1 | `stores/undo.ts` `notify()` | 防误触缺失 | 5 秒内连续删两条评论：第二次的 `notify()` 直接覆盖第一次的 `onCommit`，**第一条评论 UI 上消失但服务端从未执行删除**，刷新后"复活"；「取消关注→再操作」同理导致撤销回调丢失 | 改为队列：已有未决操作时先立即 commit 上一条再开新条 |
| 2 | `components/QuickComposer.vue` `collapse()` | 防误触缺失 | 用户写了一半标题/正文，点「取消」**内容直接清空**，无确认、无草稿 | 有内容时弹确认，或缓存草稿 |
| 3 | `views/PostCreateView.vue` `aiRun('title')` | 状态缺失 | 「AI 起标题」先 `form.title = ''` 再流式请求；**请求失败时用户原标题永久丢失** | 失败时回填 `before` |
| 4 | `views/PostCreateView.vue` | 防误触缺失 | 无 `beforeunload` 监听：浏览器刷新/关页时最后 2 秒内的输入来不及落草稿（`onBeforeUnmount` 在页面卸载时不可靠） | 增加 `beforeunload` 时同步 `saveDraft()` |
| 5 | `views/PostCreateView.vue` + `router/index.ts` | 导航断裂 | 游客可直接进入发帖页，写完点发布才跳登录——**依赖 localStorage 草稿恢复兜底**，若草稿被拒（隐私模式）整篇丢失 | 路由守卫拦截未登录，或进入即提示 |
| 6 | `views/CommunityDetailView.vue`（≥1024px 宽屏分支） | 导航断裂 | 宽屏下 `page-header`（含唯一返回按钮）被 `v-if="!isWide")` 整体隐藏，且 `/c/:id` 不在 tabbar 路由内 → **桌面用户（尤其游客）页面上没有任何导航/返回控件**，左栏"我的频道"对游客为空 | 宽屏保留顶部返回条或面包屑 |
| 7 | `views/NotificationView.vue` + `stores/notification.ts` `fetchList` | 状态缺失 | `fetchList` 无 catch，失败时 `loaded` 永远为 `false` → 页面**永久显示骨架屏**，无错误提示、无重试入口 | 失败置 error 态 + 重试按钮 |
| 8 | `views/PostDetailView.vue` `onToggleTop` / `onToggleEssence` | 无反馈 | 置顶/加精无 try/catch：失败时 Promise 静默 reject，**用户无任何反馈**；成功也无 toast | 补 try/catch + toast + loading |
| 9 | `views/HomeView.vue` `pickDefault` | 状态缺失 | 首页加载「我的频道」失败被 `.catch(() => {})` 吞掉：已登录用户看到"加入一个频道"的错误引导，无重试 | 失败态 + 重试 |
| 10 | `views/FavoritesView.vue` | 状态缺失 | 首次加载失败时 `loading=false` + 空列表 → 显示**"暂无收藏"误导性空态**，无重试 | 增加错误态（参考 MyFeedView 的实现） |

## 🟠 中严重度（明显体验受损）—— 26 项

| # | 页面/组件 | 问题类型 | 影响的用户场景 | 修复建议 |
|---|---|---|---|---|
| 11 | `views/PostDetailView.vue` 加载失败分支 | 状态缺失 | 网络错误/500 时显示"帖子不存在"，无重试，用户误以为帖子被删 | 区分错误类型，错误态 + 重试 |
| 12 | `views/CommunityDetailView.vue` / `components/ChannelWorkspace.vue` | 状态缺失 | 频道加载失败统一显示"频道不存在"，无重试 | 同上 |
| 13 | `views/UserProfileView.vue` | 导航断裂 | 返回按钮用 `$router.back()`：从分享链接/新标签页直接进入时**返回直接退出站点或无反应** | 兜底跳首页/发现页 |
| 14 | `views/UserProfileView.vue` | 状态缺失 | 用户不存在与网络错误混为一谈；且用户加载失败后仍继续 `loadPosts()` 触发报错 toast | 失败后不再请求帖子 |
| 15 | `views/PostDetailView.vue` 返回 + 面包屑 | 导航断裂 | 返回固定跳 `/c/{community_id}`：从搜索/收藏/用户主页/关注流进入时**回不到来源页**；meta 里"频道 · 版块"是纯文本不可点击 | 面包屑可点击 + history.back 兜底组合 |
| 16 | `views/CommunityDetailView.vue` 移动端返回 | 导航断裂 | 返回固定指向"发现"：从首页工作台/我的频道进入时上下文丢失 | 按来源兜底 |
| 17 | `components/Lightbox.vue` | 响应式 | **无任何 touch 事件**：手机上图片不能滑动切换、不能双指缩放（仅鼠标拖拽/滚轮），移动端图片浏览功能基本不可用 | 增加 touchstart/touchmove 手势 |
| 18 | `views/me/NotificationSettingsView.vue` | 状态缺失 | 设置加载失败静默吞掉，所有开关**默认显示为关闭** → 用户误以为自己关了通知 | 失败提示 + 重试 |
| 19 | `views/me/MyChannelsView.vue` `load()` | 状态缺失 | 无 catch：接口失败时 `loading=false` + 空数组 → 显示"还没有创建过频道"等**误导性空态** | 增加错误态 |
| 20 | `components/AiBot.vue` | 一致性/无反馈 | ① 关闭面板即清空全部对话历史，重开归零；② 流式回答中**无"停止生成"按钮**，`busy` 锁最长 60s 无法再提问 | 会话持久化 + AbortController 停止按钮 |
| 21 | `components/RichEditor.vue`（话题/链接）+ `PostCreateView`（文生图） | 一致性 | 共 4 处使用 `window.prompt()` 原生系统弹窗收集输入：移动端体验极差、打断编辑焦点、无法样式化 | 改为 TDesign Dialog/内联输入 |
| 22 | `components/RichEditor.vue` `openAt` | 无反馈 | 点击 @ 后先等接口返回才展开面板：**首次点击约 1 秒毫无反应**；成员无搜索、仅取前 50 人 | 先展开面板 + 骨架加载 + 搜索 |
| 23 | `views/PostDetailView.vue` `.post-actions` | 响应式 | 5 个操作按钮 + 评论/浏览计数，`display:flex` **无 flex-wrap**：375px 宽下横向溢出被 `overflow-x:hidden` 裁掉，举报按钮可能看不见 | `flex-wrap: wrap` |
| 24 | `views/CommunityDetailView.vue` `.msg` | 文案 | `ownerMsg` 的错误信息（"上传失败"等）也用 `.msg` 样式，**固定绿色（success）** → 失败显示为绿色成功色 | 错误用红色 |
| 25 | `views/DiscoverView.vue` 创建频道 | 防误触缺失 | 游客可见"创建频道"按钮并填写表单，点确认才跳登录，**已填内容丢失** | 未登录时点击即引导 |
| 26 | `views/PostDetailView.vue` 评论框 | 防误触缺失 | 游客输入评论点发送 → 跳登录，返回后**输入内容丢失**（组件卸载） | 本地暂存评论内容 |
| 27 | `views/PostDetailView.vue` AI 摘要/分享 | 一致性 | 「生成 AI 核心摘要」和「分享短链」对游客 `requireLogin()`：阅读是公开的，摘要却要登录，长帖游客无法使用 | 摘要放开或说明原因 |
| 28 | `views/PostCreateView.vue` AI 帮写 | 无反馈 | AI 写正文/润色/起标题流式过程无取消入口，冷启动可能等十几秒 | AbortController + 取消按钮 |
| 29 | `views/PostCreateView.vue` `tryRestoreDraft` | 文案 | 草稿恢复弹窗点"取消"即**立即删除草稿**，文案未提示此后果 | 文案明示"不恢复将删除" |
| 30 | `components/UndoSnackbar.vue` | 无反馈 | 撤销条无倒计时指示，用户不知道 5 秒后操作自动生效（评论真删） | 加进度条/倒计时 |
| 31 | `components/NewPostsPill.vue` + `useWebSocket` | 一致性 | `feed_new` 是全局广播：用户看 A 频道，B 频道新帖也让药丸显示"有 N 条新讨论"，**点击查看后刷新当前版块什么都没有** | 按当前频道过滤计数 |
| 32 | `views/DashboardView.vue` | 状态缺失/文案 | ① 总览加载失败仅显示错误文本，无重试；② 举报列表 `created_at` 原始字符串未格式化；③ 举报只拉 50 条无分页、无总数提示 | 错误重试 + formatTime + 分页 |
| 33 | `views/AdminView.vue` 操作日志 | 文案/状态缺失 | ① 只加载 50 条但"共 N 条"用本地数组长度（误导）；② `op-detail` 直接展示 `JSON.stringify(o.detail)` 原始 JSON 给管理员看 | 真实 total + 人性化展示 |
| 34 | `views/AdminView.vue` 权限失败态 | 状态缺失 | 无管理权限者进入仅底部一行小字报错，页面其余为空白无 tab 内容；路由守卫只查登录不查权限 | 明确"无权限"错误页 |
| 35 | `CommunityDetailView`/`ChannelWorkspace` 宽屏布局 + `App.vue` | 溢出遮挡 | ① `.wb-grid` 高度 `calc(100vh - nav-height)` 但宽屏下**根本没有 nav 头**，底部留 52px 空白；② `App.vue .app-shell` 无条件 `padding-bottom: tabbar-height`，所有全屏页（帖子/频道/发帖）底部多 56px 空白 | 按实际头部/有无 tabbar 计算 |
| 36 | `views/HomeView.vue` 频道记忆 | 状态缺失 | `sdu_last_channel_id` 指向已退出/已解散频道时，首页直接"频道不存在"死胡同，移动端无任何切换入口（宽屏才有左栏） | 记忆失效时自动回退 `mine()` 第一个 |

## 🟡 低严重度（细节瑕疵）—— 24 项

| # | 页面/组件 | 问题类型 | 影响的用户场景 | 修复建议 |
|---|---|---|---|---|
| 37 | `views/DiscoverView.vue` 搜索结果 | 导航断裂 | 无 `<keep-alive>`：从搜索结果点进帖子返回后，**关键词、结果、滚动位置全部丢失**，需重新搜索（所有列表页同病） | keep-alive 或状态提升到 store |
| 38 | 搜索结果卡片 / `FeedCard` / `ChannelGroup` 卡片 | 可访问性 | 可点击卡片用 `div + @click` 而非 `<a>`：键盘无法 Tab/Enter 打开，屏幕阅读器不可导航 | 改为 router-link |
| 39 | `views/MeView.vue` | 文案 | 账号卡 `@{{ accountName }}` 用昵称拼接——`@` 后应为 username；`auth.user` 未加载瞬间闪现"未登录" | 用 username + 加载态 |
| 40 | `views/MeView.vue` | 一致性 | 本身是 tab 页（底部有"我的"），顶部却又有"返回"链接，两套返回语义重复 | 移除顶部返回 |
| 41 | `views/auth/LoginView.vue` / `RegisterView.vue` | 导航断裂 | 登录/注册页无任何返回首页/其他页的入口（无 logo 链接） | 顶部加品牌→首页链接 |
| 42 | `views/auth/*` | 防误触缺失 | 登录/注册密码框无"显示密码"开关；注册无确认密码字段、无强度提示；验证码倒计时刷新页面即丢 | 常规补齐 |
| 43 | `views/auth/RegisterView.vue` | 文案 | 所有校验错误共用表单底部一行文字，不与具体字段关联，不滚动到错误处 | 字段级内联错误 |
| 44 | `views/PostDetailView.vue` 评论/回复输入 | 无反馈 | `maxlength=2000` 无字数计数；标题 128、快评 5000 同样无计数 | 显示剩余字数 |
| 45 | `views/PostDetailView.vue` 附件 | 无反馈 | 附件图片若不在帖子图库内，点击无任何反应（注释自述"保持旧行为"） | 至少独立预览 |
| 46 | `views/PostDetailView.vue` `.att-del` | 响应式 | 删除按钮绝对定位 `top:-6px/right:-6px` 超出卡片边界，窄屏易与相邻元素重叠/误触 | 放入卡片内 |
| 47 | `views/NotificationView.vue` | 响应式/无反馈 | ① 未读红点（absolute, right:10px）与右侧"删除"按钮区域可能重叠；② 无 `ref_id` 的通知点击后**无任何反应也无提示** | 位置避让 + 无跳转时 toast |
| 48 | `views/NotificationView.vue` `fmtTime` | 一致性 | 本文件自实现相对时间，与 `utils/time.ts` 的 `timeAgo` 行为不一致（>7 天显示逻辑不同） | 统一用 utils |
| 49 | `index.html` | 文案 | 页面标题"SDUdiscord · **仿腾讯频道**"——面向用户的标题含开发内部措辞 | 去掉"仿腾讯频道" |
| 50 | `App.vue` / `MyFeedView.vue` | 文案 | `console.log/warn/error` 遗留（"[SDUdiscord] 后端连通"等） | 移除或降级 |
| 51 | `DiscoverView` 热门频道条 / `ChannelWorkspace` 频道 chips | 响应式 | 横滑区域隐藏滚动条且**无任何可滑动提示**；名称截断 60–64px 仅 `title` 属性（触屏不可见） | 渐变遮罩/箭头提示 + 长按提示 |
| 52 | `views/CommunityDetailView.vue` 成员/待审 | 状态缺失 | 成员只拉前 50 无"查看更多"（大频道静默截断）；"N 条待审"用本地数组长度（≤50），与实际待审数不符 | 分页 + 用 total |
| 53 | `views/CommunityDetailView.vue` 移动端 | 一致性 | 频道主不能"退出"（须解散）但按钮直接消失，无任何解释文案 | 提示"频道主需解散频道" |
| 54 | `views/CommunityDetailView.vue` 加入审核 | 防误触缺失 | "通过/驳回"申请无二次确认，驳回对申请人不可逆且无通知预览 | 驳回加确认 |
| 55 | `FeedStreamList`/社区信息流 去重逻辑 | 状态缺失 | 置顶帖每页重复返回被 `filter(!seen)` 剔除后可能整页 0 条：点"加载更多"**列表无变化**，用户以为卡死 | 空结果时自动续拉下一页 |
| 56 | `views/DashboardView.vue` 举报/敏感词 | 防误触缺失 | 举报"办结/驳回"、敏感词删除均无确认弹窗 | 补确认 |
| 57 | `views/DashboardView.vue` 趋势图 | 一致性 | 纯 CSS 柱状图无坐标轴/悬停提示，仅柱顶数字；"导出 CSV"无 loading | 图表增强 |
| 58 | `components/FeedCard.vue` `measureOverflow` | 一致性 | 溢出检测只在 mount/resize 时执行：懒加载图片、字体加载后高度变化可能使"展开全文"判断失准 | 图片加载后重测 |
| 59 | `components/FeedCard.vue` Lightbox | 溢出遮挡 | 每张卡片各自挂载一个 Teleport Lightbox 实例（一页 20+ 个），纯性能浪费，弱机卡顿 | 全局单例灯箱 |
| 60 | `composables/useGlobalShortcuts.ts` | 无反馈 | J/K// 快捷键无发现性：无任何提示入口，用户不知道存在；无 feed 时按 J/K 无反馈 | 帮助弹层/悬浮提示 |

---

## 统计汇总

| 严重度 | 数量 | 主要集中区域 |
|---|---|---|
| 🔴 高 | 10 | 内容丢失（#1–#5）、桌面导航断裂（#6）、失败态缺失（#7/#9/#10） |
| 🟠 中 | 26 | 错误态误报为"不存在"、原生 prompt、移动端手势、宽屏布局空隙 |
| 🟡 低 | 24 | 一致性、可访问性、文案、计数/分页细节 |
| **合计** | **60** | |

### 按问题类型分布

| 类型 | 数量 |
|---|---|
| 状态缺失（加载/空/错误四态不全） | 18 |
| 导航断裂（返回/面包屑/死胡同） | 9 |
| 防误触缺失（内容丢失/无确认） | 13 |
| 无反馈（操作无响应/无提示） | 9 |
| 文案（误导/开发者措辞/颜色错误） | 8 |
| 响应式（移动端/溢出/裁切） | 7 |
| 一致性（行为/时间格式/术语不统一） | 8 |
| 可访问性 | 1 |

### 优先修复建议（按投入产出比）

1. **先修 5 个内容丢失类**（#1 #2 #3 #4 #5）——用户数据丢失是最严重体验事故
2. **统一"失败 ≠ 不存在"错误态**（#7 #9 #10 #11 #12 #19）——可抽一个 `ErrorState` 组件全局复用
3. **补桌面宽屏导航**（#6 #35）——桌面是当前主要使用场景
4. **Lightbox 触摸手势**（#17）——移动端图片浏览不可用
5. **替换 4 处 `window.prompt`**（#21）——一次改造显著提升发帖体验

> 本报告仅罗列问题，未做任何代码修改。需要修复时请回复编号（如 "1、2、7" 或 "全部高危"）。
