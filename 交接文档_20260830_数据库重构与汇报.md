# 工作交接文档 —— 数据库结构重构 + 验收环境 + 答辩汇报策划（2026-08-30）

> 承接文档：`工作交接文档.md`（2026-08-24，开发阶段全貌，本篇不重复其内容）
> 上一份承接会话：第一轮数据库优化（AI审查报告3_数据库优化.md + 迁移 d0e1f2a3b4c5→f9a2b4c6d8e0 初版 + 133 测试全绿），当时状态"等待 push 部署"。
> 本会话在其上完成了**第二轮结构重构（用户批准的四项大改）**、**验收环境搭建**、**爬取数据质量评估**、**答辩汇报策划（第一部分已展开）**。

---

## 0. 一句话现状

数据库已完成第二轮大刀阔斧重构（likes 拆表 / posts 垂直拆分 / 版块白名单规范化 / 通知 ref_type），迁移文件 `f9a2b4c6d8e0` 在**空库 + 真实数据副本**上完整回环验证通过（过程中抓出 2 个 downgrade 顺序 bug 并已修复），133 个测试全绿；**全部改动仍在工作区未提交**（push 会触发自动部署）；服务器上已搭好独立验收库 `guild_accept`（真实数据+新结构）供用户验收；答辩汇报已完成基调设计与第一部分（功能需求分析）内容展开，第二三部分待续。

---

## 1. 数据库结构现状（权威清单，汇报/答辩/继续开发以此为准）

### 1.1 表：31 张物理表（30 张 ORM 模型 + 1 张迁移直建台账）

**五大业务域聚类**（答辩叙事骨架，勿拆散）：

| 域 | 表 |
|---|---|
| 内容域（7） | users, communities, boards, posts, **post_contents**(新), comments, topics |
| 关系域（8） | members, roles, join_requests, follows, user_follows, **post_likes**(新), **comment_likes**(新), favorites |
| 治理域（5） | notifications, reports, reviews, sensitive_words, op_logs |
| 运营域（5） | ai_call_logs, daily_stats, search_records, short_links, feed_strategies |
| 配置+基建（6） | ai_configs, system_configs, **post_embeddings**(向量), **board_role_perms**(新), counter_audit(迁移直建无模型), alembic_version(工具) |

### 1.2 本轮重构前后的变化（原 26 业务表 → 30 模型表）

- `likes`（多态 0 哨兵表，post_id/comment_id 双 0 填充）**已删除** → 拆为 `post_likes`（UNIQUE(post_id,user_id)，FK posts CASCADE + users CASCADE）和 `comment_likes`（UNIQUE(comment_id,user_id)，FK comments CASCADE + users CASCADE）
- `posts` 的 rich_content / source_markdown / images 三大列**已删除** → 垂直拆入 `post_contents`（post_id 主键即 FK，CASCADE）；FULLTEXT 索引随之拆为 `ft_post_title`(posts.title) + `ft_post_content`(post_contents.source_markdown)，双路 ngram
- `boards.allow_post_role_ids`（JSON 数组）**已删除** → `board_role_perms` 复合主键关系表（board_id+role_id 双 FK CASCADE）；空白名单=所有人可发帖
- `notifications` 新增 `ref_type VARCHAR(32) NULL`（post/comment/community/user），7 处 notify 调用点全部显式传入
- `post_embeddings`（上一轮已建）：AI 向量 JSON 独立表

### 1.3 数据库对象（迁移 f9a2b4c6d8e0 全量清单）

| 对象 | 数量 | 明细 |
|---|---|---|
| 外键 | **45** | 全部非多态关系；级联三策略：CASCADE=从属数据 / SET NULL=弱引用 / RESTRICT=作者类强引用 |
| 视图 | 6 | v_post_card, v_hot_posts, v_member_profile, v_community_overview, v_pending_reports, v_ai_degraded_calls |
| 触发器 | 12 | 全部对账型：计数器变更记 delta 进 counter_audit，不直接改计数器（防与应用层双计）。likes 拆表后 4 个点赞触发器（trg_post_likes_ai/ad, trg_comment_likes_ai/ad）不再需要分支判断 |
| 存储过程 | 1 | sp_reconcile_counters()：以源表为事实源校准 9 个计数器（posts.like_count/favorite_count/comment_count、comments.reply_count/like_count、communities.member_count/post_count、topics.post_count/heat_value），幂等 |
| 函数 | 1 | fn_post_heat(post_id)（like+2*comment+3*favorite+置顶100，与 heat_service 权重一致） |
| 事件 | 4 | ev_clean_short_links(日4点) / ev_release_shutup(每小时) / ev_reconcile_counters(日5点，含 counter_audit 90天清理) / **ev_clean_notifications(日4:30，90天已读通知)** |
| 全文索引 | 2 | ft_post_title(posts.title, ngram)、ft_post_content(post_contents.source_markdown, ngram) |

### 1.4 计数器对账口径（改计数逻辑前必读）

- post_count = status<>1 的帖子数（封禁 status=2 不扣减，与 review 流程一致）
- heat_value = 全部状态帖子数（历史累计热度：发帖+1 永不回减）
- comment_count/reply_count = status=0 的行数
- like_count = post_likes / comment_likes 物理行数
- member_count = members 物理行数
- **任何导入外部数据/绕过服务层写库后，跑一次 `CALL sp_reconcile_counters()` 即可全部校准**

---

## 2. 本会话完成的工作（按发生顺序）

### 2.1 第二轮审查修复（小问题逐条）

1. **4 处非原子计数**（与当年 like_count 同类 bug）：community_service 加入/退出频道的 member_count、post_service 发帖 topic.post_count/heat_value、level_service 的 member.level —— 全部改 SQL 原子写（func.greatest 防负）
2. **死列激活**：members.last_active_at（生产 159 行全 NULL）挂到 add_level，与等级分同一原子语句写入
3. **ai_configs 装饰品变真开关**：管理端可配置但网关从不读取 → llm_gateway 加 `_ensure_feature_enabled(feature)`（60s 进程内缓存，空表默认开启，关闭抛 FeatureDisabledError→403）；`app/core/response.py` 新增该异常类（code=2003）
4. **join_requests 状态机闭环 bug（真缺陷）**：`uq_joinreq_community_user` 唯一键是"一人一频道终身一条"，但防重只拦 PENDING → 被拒后重新申请撞唯一键 500。修复：无记录→新建；审核中→409；被拒→**复用原行原子重置**（UPDATE ... WHERE status=REJECTED，handler_id/handled_at 置空），并发安全。新增回归测试 `test_rejected_join_can_reapply`
5. 迁移增强：对账过程补 topics.heat_value 校准；对账事件追加 counter_audit 90 天清理

### 2.2 用户批准的四项结构重构（"OK都同意，开始执行"）

| 项 | 内容 | 涉及代码 |
|---|---|---|
| A | likes 拆表 | app/models/like.py 重写（PostLike+CommentLike）；interact_service 点赞/取消分支；comment_service.comment_out；post_service.post_out/post_outs；admin_service/stats_service 计数；迁移触发器 2→4 个；对账 SQL 改源表 |
| B | posts 垂直拆分 | 新模型 app/models/post_content.py；post_service 创建（db.flush 后写 PostContent）/编辑/输出（post_out 单查 + post_outs 一次 IN 批量，PostOut 三字段给默认值再回填）；search_service._fulltext_ids 改双 FT UNION、_like_posts 改 join；ai/review.py、ai/rag.py、ai/summary.py 全部改读 post_contents |
| C | 白名单规范化 | 新模型 app/models/board_role_perm.py；community_service.create_board（flush 后插关系行）/update_board（全量替换）/community_out 批量聚合/_board_out helper；post_service._check_board_post_perm 改查关系表；BoardOut.allow_post_role_ids 默认值保持 API 形状 |
| D | 通知健壮性 | notifications.ref_type 列；notify_service.notify 加参数透传；7 个调用点（interact/comment/post/report/review）传 ref_type；ws/events.py payload 加字段；迁移回填 CASE type→ref_type；新事件 ev_clean_notifications |

### 2.3 迁移文件重写与验证（f9a2b4c6d8e0 原地修改，未上生产）

验证链：空库回环 ✓ → 真实数据演练（备份导入 guild_mig_data）✓ → downgrade→upgrade 回环 ✓。最终：31 物理表 / 45 外键 / 6 视图 / 12 触发器 / 4 事件 / 2 例程。

**真实数据演练抓出 2 个 downgrade 顺序 bug（已修复，空库不可能发现——答辩可讲）**：
1. phone 回滚：多行 NULL 同转 '' 撞 uq_users_phone 1062 → 正确顺序=**先 drop 唯一约束 → 再 UPDATE 清 NULL → 最后 MODIFY NOT NULL**
2. error 列 512→255 收窄：超长数据 1406 → 收窄前先 `UPDATE ai_call_logs SET error = LEFT(error, 255)`

### 2.4 测试适配

- post_service 补 `from app.models.post_content import PostContent`（曾遗漏导致 NameError，133→全绿）
- test_content_api.py::test_board_post_role_restriction：原用悬空 role_id=9999（JSON 时代可行），外键化后被 1452 拒绝 → 改为先 POST /roles 创建真实"贵宾"身份组再引用（**外键化的红利案例，答辩素材**）
- 最终全量 133 tests exit=0（连跑 3 次稳定）

### 2.5 验收环境（已交付用户，等验收反馈）

- 服务器建 **guild_accept** 库：备份真实数据（113帖/37用户/214评论/710赞/60版块）+ 新结构（45外键/6视图/12触发器/4事件）
- 验收指南：**`D:\DBS-curdeg\验收指南_数据库优化.md`**（连接方式=本地 `ssh -N -L 13306:127.0.0.1:3306`，Navicat 连 127.0.0.1:13306 / guild / 密码同 backend/.env；A 结构验收 / B 数据核对 / C 机制演示（触发器台账、对账幂等、ngram 中文搜索、热度函数）/ D 可选功能验收）
- 用户**尚未验收**；验收后要回答 4 个问题：①汇报载体 PPT 还是 Word ②时长 ③图工具（PlantUML 源码已备）④下一轮产出物

### 2.6 爬取数据质量评估（migrant_data/，外部 AI 继续抓取中）

结论 **B+ 可迁移**。已交评估：优点（raw 逐页存档可审计、时间双存、112 用户同 ID 异昵称=0、楼中楼结构完备可直映射 reply_to_user_id、列表接口正文不截断 717 字验证）；必改 3 项（评论页数上限放开——52 评论只抓 15、改按版块流抓取——主页流 6/10 帖无版块归属、图片外链带签名会过期需本地化）；字段映射方案已给（channel→communities、users 合成 username=u_<id后10位>、楼中楼→parent_id+reply_to_user_id、title 首行生成、feed_type 不可信按 media 判定 post_type）；**6 个决策点已给推荐**（点赞归零/真实 member_count+简介注明原规模/图片本地化/status=0 合成用户/先 3-5 大频道/专用脚本直写 DB+reconcile 收尾）。**尚未写迁移脚本**。

### 2.7 答辩汇报策划（进行中，详见 §5）

---

## 3. 工作区 git 状态（未提交，push=自动部署，慎重）

**本会话/上会话的改动（数据库优化全部工作）**：
- backend/alembic/versions/f9a2b4c6d8e0_db_optimization_fk_views_triggers_proc_event.py（重写，新增未跟踪）
- backend/app/models/：like.py 重写、post.py/board.py 删列、notification.py 加 ref_type、__init__.py、新增 post_content.py / board_role_perm.py / post_embedding.py（上轮）
- backend/app/services/：post_service / community_service / interact_service / comment_service / level_service / admin_service / stats_service / search_service / notify_service / report_service / ai_call_log_service
- backend/app/ai/：llm_gateway（开关+usage 回填）、review、rag、summary
- backend/app/core/：response.py（FeatureDisabledError）、config.py、main.py（BACKGROUND_TASKS_ENABLED 守护）
- backend/app/ws/events.py、backend/app/schemas/post.py、community.py
- backend/tests/：conftest、test_community_api（+重申回归测试）、test_content_api（外键化适配）、test_rbac/test_semantic/test_short_link
- deploy/docker-compose.yml（event-scheduler=ON）、deploy/backup.sh（新增未跟踪）
- 根目录：AI审查报告3_数据库优化.md、验收指南_数据库优化.md（新增）

**用户自己的改动（与本次无关，勿动勿混提交）**：app/api/v1/members.py、users.py、frontend/src/api/community.ts、post.ts、components/ChannelWorkspace.vue、views/{Admin,Discover,Home,Me,UserProfile}View.vue、tests/test_user_profile_posts.py（未跟踪）、designer/、migrant_data/、guild_backup_20260829_110228.sql 等

---

## 4. 关键决策记录（含理由，答辩 Q&A 素材）

1. **likes 拆表**：多态 0 哨兵是全库唯一无法外键化的表；拆后 45 外键全覆盖，"多态反模式清除"答辩可讲。触发器不再分支判断。
2. **垂直拆分 post_contents**：主表行宽解耦（feed keyset 扫描缓冲页命中率），FT 随迁；PostOut 三字段给默认值再由服务层回填，API 契约不变。
3. **board_role_perms 规范化但 roles.perms 保留 JSON**：引用型白名单外键化，权限点集合小且稳定保留 JSON——"何时规范化/何时 JSON"的权衡叙事。
4. **对账型触发器**（非直改计数器）：应用层已维护计数器，触发器直改会双计；delta 台账+EVENT 每日校准=最终一致闭环。
5. **join_requests 保留唯一约束+应用层闭环**（而非去约束）：一人一频道一条记录，被拒不删行而重置——原子防并发。
6. **AI 开关空表默认开启**：兼容历史行为与测试库。
7. **爬取数据导入策略（已推荐待用户确认）**：专用脚本直写 DB 绕过服务层 → `CALL sp_reconcile_counters()` 一次性校准 → 点赞归零（保住对账闭环）/member_count 用真实数。
8. **演进项（明确不做）**：MySQL 8.0 升级、likes 合并回多态、日志表分区（主键含分区列与 FK 冲突）。

## 5. 事故与教训（新会话必读，避免重蹈）

1. **备份文件 `guild_backup_20260829_110228.sql` 第 24 行有 `USE \`guild\`;`**——直接导入会写穿到生产库！唯一安全姿势：`grep -v "^USE" backup.sql | mysql ... guild_xxx`（在**本地** grep 后经 ssh 管道传入）。上一会话曾因此误重建生产库（损失仅 cron 统计行，已自愈）。
2. **远端服务器没有 /opt/channel/backup/ 目录**——备份文件只在本地 D:\DBS-curdeg\。导入必须走"本地 grep → ssh 管道 → docker exec -i"模式。本会话曾两次误报"生产库被重建"，最终核实是导入根本没执行（远端重定向失败），guild 全程未被动过。**遇到异常先看信息来源是否真实发生**。
3. **生产库 guild 现状**：仍是优化前结构 d0e1f2a3b4c5（26 业务表+alembic_version），数据=备份快照+cron 自愈统计行。迁移 f9a2b4c6d8e0 未上生产（等 push）。
4. **bash 多层转义陷阱**：heredoc 里写含 `\n` 字面量的补丁字符串会被吞反斜杠（本会话 rag.py 补丁失败 3 次）。对策：补丁脚本用 write 工具落盘成 .py 再执行，或用 `chr(92)+'n'` 构造。
5. **deploy docker-compose 的 mysql 只回环绑定**：`127.0.0.1:3306:3306`，公网不可达；本地访问一律走 SSH 隧道本地 13306。

## 6. 环境与命令备忘

```powershell
# SSH 免密
ssh -i C:\Users\33031\.ssh\id_ed25519_deploy root@122.152.219.236
# 隧道（用户验收用）
ssh -i C:\Users\33031\.ssh\id_ed25519_deploy -N -L 13306:127.0.0.1:3306 root@122.152.219.236

# 后端测试（需隧道开着；同时只能跑一个 pytest）
cd D:\DBS-curdeg\backend && python -X utf8 -m pytest -q
# 当前 133 个测试全绿

# 迁移演练（MYSQL_DATABASE 覆盖目标库；库需先建好并 GRANT 给 guild@%）
cd D:\DBS-curdeg\backend
$env:MYSQL_DATABASE="guild_xxx"; python -X utf8 -m alembic upgrade head / downgrade d0e1f2a3b4c5

# 服务器执行 SQL（标准姿势）
ssh ... 'source /opt/channel/deploy/.env; docker exec deploy-mysql-1 mysql -uroot -p"$MYSQL_ROOT_PASSWORD" -e "SQL" 2>&1 | grep -v insecure'

# 导入备份到任意演练库（唯一安全姿势，在本地 grep）
grep -v "^USE" D:\DBS-curdeg\guild_backup_20260829_110228.sql | ssh ... 'docker exec -i deploy-mysql-1 sh -c "mysql -uroot -p\"\$MYSQL_ROOT_PASSWORD\" 库名"'

# 验收库（现成）：guild_accept —— 可随时重建：DROP→CREATE→GRANT→导入→alembic upgrade head
```

- 服务器部署：/opt/channel/{backend,deploy,frontend,...}，deploy/docker-compose.yml（mysql:5.7 + redis + backend + nginx/caddy）
- 生产库连接参数在 /opt/channel/deploy/.env（MYSQL_ROOT_PASSWORD / MYSQL_PASSWORD / MYSQL_USER=guild）
- git push main → 每 3 分钟 cron 自动部署 → 容器启动自动 `alembic upgrade head`（**这就是迁移上生产的时机**）

## 7. 待办与下一步

### 7.1 答辩汇报（进行中，优先）
- **基调已定**：「以数据为中心设计社区系统」——功能需求收敛为数据实体与关系，非功能需求落地为数据库机制。三原则：需求可验证（123 API 端点/25 服务/133 测试）、设计有故事（ER 演进：范式化→多态反模式清除→拆表→外键全覆盖）、质量有机制（外键+台账+对账+备份）
- **15 页结构已定**（见 §8），第一部分**功能需求分析已完整展开**（上 session 末尾输出）：双产品竞品矩阵切入页（Discord/腾讯频道/本系统三列）、Actor 分析（游客/注册用户/频道管理员/系统管理员/AI 虚拟账号 user_type=2，泛化链）、8 模块功能树（123 端点标注）、用例图 PlantUML 总图（含 include/extend 三处业务含义）、"发布帖子"完整用例规约、"加入频道"状态机页
- **待做**：第二部分性能及其他需求（5分，"需求→机制"对照表：Redis/keyset/复合索引/ngram/JWT/RBAC/对账/备份/AI 开关）；第三部分数据库设计（15分，重头：设计流程页、五域 ER 总览、核心域 ER（1:1 垂直拆分/1:n/自反楼中楼）、关系域 ER（m:n 转关系表——得分核心）、治理+运营 ER、物理设计页（45 外键三策略/索引依据/范式与反范式权衡）、数据质量闭环压轴页（counter_audit 台账→事件对账→爬取数据导入校准）、成果数字一览）；ER 图四张 PlantUML/mermaid 源码；讲稿与预答辩 Q&A 弹药（"为什么 31 张表""为什么点赞拆表""JSON vs 规范化"已有标准答案，见 §4）
- **等用户验收后回答**：①PPT 还是 Word ②时长（按 8-12 分钟/15 页设计）③图工具（PlantUML 源码已给用例图）④是否需要讲稿

### 7.2 用户验收（等待中）
- guild_accept 环境 + 验收指南已交付；用户验收中若发现不符预期会反馈
- 可选：用户开口要就搭功能验收后端（当前代码连 guild_accept、独立端口如 8100、docker 起临时容器，不碰生产）

### 7.3 爬取数据全量迁移（外部 AI 抓数据中）
- 抓取侧 3 必改已交底：评论页数放开、按版块流抓、图片同步下载本地化
- 6 决策点推荐已给（见 §2.6/§4.7），等用户确认后写 `migrant_data/migrate_pilot.py`（直写 DB + reconcile 校准 + MAPPING.md 字段映射文档）

### 7.4 部署（用户决策）
- 工作区改动提交/推送 = 触发自动部署 = 迁移 f9a2b4c6d8e0 上生产（已在真实数据副本完整演练）。**建议先让用户验收 guild_accept，认可后再 push**。push 前注意工作区混有用户自己的前后端改动（见 §3），建议分开提交。

## 8. 附：答辩汇报 15 页结构（已定稿，第二三部分待展开）

P1 封面 ｜ P2 竞品矩阵切入 ｜ P3 Actor 分析 ｜ P4 功能结构图 ｜ P5 用例图总图 ｜ P6 用例规约"发布帖子" ｜ P7 用例规约"加入频道"状态机 ｜ P8 性能需求（机制对照）｜ P9 安全/可靠/可扩展 ｜ P10 设计流程与五域总览 ｜ P11 核心域 ER ｜ P12 关系域 ER（m:n 得分页）｜ P13 治理+运营 ER ｜ P14 物理设计（外键三策略/索引/范式权衡）｜ P15 数据质量闭环（压轴）+ 成果数字一览

**成果数字一览素材**：31 张表（五域聚类 7/8/5/5/6）、45 外键、6 视图、12 触发器、1+1 例程、4 事件、2 组 ngram 全文索引、123 API 端点、25 服务模块、133 测试全绿、31→45 外键全覆盖（多态列仅剩 3 处文档化例外：op_logs/reports/short_links 的 target_id + notifications.ref_id 弱引用）。
