# 工作交接文档 —— dbs-curdeg（仿「腾讯频道」课设项目）

> 生成时间：2026-08-19 ｜ 用途：会话切换后，新会话无需重新摸索即可接手
> 本机工作目录：`D:\DBS-curdeg` ｜ GitHub：https://github.com/Dc-D666/dbs-curdeg（私有）

---

## 0. 一句话总结

一个「仿腾讯频道」大学生课设项目（Web + Android 双端，共用 FastAPI API），**开发→部署全链路已经打通**：本地 `git push` 到 GitHub 私有仓库，服务器每 3 分钟自动拉取并重建，站点跑在 **https://guild.weaxi.cn**（HTTPS 已配好自动续期）。**业务代码还没开始写**，下一步是开发账号系统。

---

## 1. 项目定位

- **做什么**：模仿腾讯频道（QQ 频道）私域平台——频道→版块→帖子→评论/点赞→身份组→治理→AI 功能
- **技术栈**（已在 `课设技术栈推荐与实施方案.md` 定稿）：
  - 后端：Python 3.12 + FastAPI + SQLAlchemy + MySQL 8 + Redis 7 + WebSocket
  - 前端：Vue3 + Vite（移动优先 H5 + PC 管理后台）
  - Android：Kotlin + Jetpack Compose（**尚未开始**）
  - AI：DeepSeek（LLM 网关、AI 帮写、内容巡检、RAG 问答机器人）
- **调研资料**（在仓库里）：`research_tencent_channel_public.md`、`research_cli_mcp_native_fields.md`、`课设技术栈推荐与实施方案.md`、`开发部署工作流方案.md`（注意：后一份文档的部署章节仍是旧方案，见 §8）

---

## 2. 当前已上线的东西（验收点）

| 项目 | 状态 |
|---|---|
| 线上站点 | **https://guild.weaxi.cn**（HTTP 自动 301 跳转 HTTPS） |
| 健康检查 | `https://guild.weaxi.cn/healthz` → `{"status":"ok",...}` |
| API 测试 | `https://guild.weaxi.cn/api/v1/ping` → `{"message":"pong"}` |
| 前端骨架 | `https://guild.weaxi.cn/` → Vue 骨架页（含后端连通性测试） |
| HTTPS 证书 | Let's Encrypt（certbot），有效期至 2026-11-17，自动续期已配置 |

---

## 3. 关键信息速查表

| 项 | 值 |
|---|---|
| 新服务器（课设用） | **122.152.219.236**（root，OpenCloudOS 9.6，实为 2 核 / 3.5G 内存——用户以为租的是 4C4G，值得再确认） |
| 旧服务器（其他项目） | 49.232.252.213（greendoc / Sound-Bridge / PatPlayer / QQ 等旧项目，勿动） |
| 域名 | `guild.weaxi.cn` → A 记录 `122.152.219.236`（DNSPod，注意：记录类型必须选 **A**，选"显性URL"不生效） |
| GitHub 仓库 | `Dc-D666/dbs-curdeg`（私有） |
| 本地 git 凭证 | HTTPS + Windows 凭证管理器（Git Credential Manager） |
| SSH 主机配置 | dsh-ssh 插件已配好两台服务器（侧边栏「SSH」入口） |

---

## 4. 部署架构（最终版，重要）

```
本地 git push → GitHub（私有仓库）
                    ↑
服务器 cron（每 3 分钟）→ /opt/channel/deploy/auto-deploy.sh
    ├─ git fetch origin main（部署密钥拉取，见 §5）
    ├─ 有变化才：git reset --hard → docker compose up -d --build → prune
    └─ 日志：/var/log/auto-deploy.log
```

**为什么不用 GitHub Actions 直接部署（重要背景）**：腾讯云安全组件「云镜 YunJing」的全球黑名单（iptables INPUT 第 1 条 DROP 规则，ipset `YJ-GLOBAL-INBLOCK` 含 1 万+ IP，含 GitHub Actions 的 Azure 段）会**静默丢弃 GitHub 运行器的入站连接**，Actions 的 SSH 永远连不上服务器（5 次运行全部失败已验证）。所以改成**服务器主动出站拉取**，绕开入站依赖。GitHub Actions 现在只做纯测试（`.github/workflows/deploy.yml`，无 SSH）。

**容器编排**（`deploy/docker-compose.yml`，在 `/opt/channel/deploy/` 下执行）：
- `web`：nginx（前端静态 + 反代 `/api/`、`/healthz`、`/ws`、`/uploads/`），对外 80/443
- `api`：FastAPI（`backend/` 构建）
- `mysql`：5.7（卷 `mysql_data`，buffer pool 384M）
- `redis`：7-alpine（卷 `redis_data`）
- 共享卷 `uploads`（用户上传文件，nginx 直接服务）

**HTTPS 机制**：
- 证书：`/etc/letsencrypt/live/guild.weaxi.cn/`（certbot，webroot 模式，webroot 卷 `/var/www/certbot`）
- nginx 读的是 `/opt/channel/deploy/certs/`（容器内挂载为 `/etc/nginx/cert`），**续期挂钩**（renewal conf 的 `renew_hook`）会自动 cp 证书 + `docker exec deploy-web-1 nginx -s reload`
- `certbot renew --dry-run` 已验证通过

---

## 5. 服务器明细（122.152.219.236）

**软件**：Docker 29.7.2 + Compose v5.5.0 + git + certbot 2.8.0 + crond（已运行）

**Docker 国内加速**（`/etc/docker/daemon.json`）：
```json
{ "registry-mirrors": ["https://mirror.ccs.tencentyun.com", "https://docker.m.daocloud.io"] }
```
（拉镜像 80s → 10s；后端 Dockerfile 用清华 pip 源、前端用 npmmirror，都是为国内构建）

**密钥/凭据（都在服务器上）**：
- `/root/.ssh/github_deploy`：**部署密钥**（read-only），公钥已加到 GitHub 仓库 Deploy Keys，用于服务器拉代码——**这是部署链路的核心，别删**
- `/root/.ssh/ci_key`：CI 密钥（公钥在 authorized_keys），原为 GitHub Actions SSH 用，**现在已无用**，可删可留
- `/opt/channel/deploy/.env`：生产环境变量（含随机生成的 `MYSQL_ROOT_PASSWORD`，**不入库**，别泄露别删）
- `/root/.ssh/config`：github.com 走 `github_deploy` 密钥

**crontab**（root）：
```
*/3 * * * * /opt/channel/deploy/auto-deploy.sh >> /var/log/auto-deploy.log 2>&1
```
（另有腾讯云 stargate 自带条目，勿动）

**防火墙**：22/80/443 开放；iptables 第 1 条为云镜黑名单 DROP（见 §4）

---

## 6. 代码仓库与本地环境

**仓库结构**（全部已推送）：
```
D:\DBS-curdeg\
├── backend/            # FastAPI 骨架：app/main.py（/healthz、/api/v1/ping），requirements.txt，Dockerfile
├── frontend/           # Vue3+Vite 骨架：src/App.vue（连通性测试页），Dockerfile（多阶段），nginx-web.conf
├── deploy/             # docker-compose.yml、nginx 配置、deploy.sh（手动一键）、auto-deploy.sh（cron 用）、.env.example、certs/、certbot/
├── .github/workflows/  # deploy.yml = 纯测试 CI
└── 各 .md 方案/调研文档
```

**git 提交历史**（6 个，全部在 main）：
```
f416fff ci: GitHub Actions 改为纯测试，部署由服务器 cron 自动完成
6838294 docs: README 添加线上地址
1baccf5 feat: 启用 HTTPS（443 + HTTP 重定向），证书挂载目录就绪
af65747 feat: nginx 增加 certbot 校验目录，compose 预留 443 与证书挂载
4f93b8f chore: 生成前端 package-lock.json，Dockerfile 改用国内镜像源
465a130 chore: 初始化 monorepo 骨架（backend/frontend/deploy/CI）
```

**本机 git 配置（已持久化，推送必须依赖）**：
```
http.sslBackend = schannel        # 直连被墙时用 schannel 能通
http.version    = HTTP/1.1
http.proxy      = http://127.0.0.1:7897   # 用户 Clash 代理，推送必须开着
```
> ⚠️ **推送前确认代理软件（127.0.0.1:7897）在运行**，否则 git push 会超时/被重置。

**dsh-ssh 插件**：已配两台服务器（密码认证），新会话可直接 `ssh_exec`/`ssh_upload` 操作 122.152.219.236。

---

## 7. 常用操作命令

```bash
# 本地开发（热更新）
cd D:\DBS-curdeg\backend && pip install -r requirements.txt && uvicorn app.main:app --reload
cd D:\DBS-curdeg\frontend && npm install && npm run dev

# 部署（一般不用手动，cron 自动；手动触发：）
ssh 122.152.219.236  # 然后
bash /opt/channel/deploy/auto-deploy.sh   # 立即同步一次
tail -20 /var/log/auto-deploy.log          # 看部署日志
docker compose -f /opt/channel/deploy/docker-compose.yml ps   # 看容器

# 证书
certbot renew --dry-run                     # 模拟续期（已配 renew_hook 自动同步+reload）

# 验证
curl -sk https://guild.weaxi.cn/healthz
```

---

## 8. 已知问题与注意事项（新会话必读）

1. **`开发部署工作流方案.md` 的部署章节写的是旧方案**（GitHub Actions SSH 部署），与实际落地（服务器 cron）不符，有空应更新，避免误导。
2. **推送网络**：GitHub 直连经常被重置，必须走代理（schannel + HTTP/1.1 + 127.0.0.1:7897，已配好）。代理软件没开 → 推送失败，不是代码问题。
3. **DNS 记录类型**：`guild.weaxi.cn` 必须用 **A 记录**（DNSPod 的「显性URL」选项不会产生真实 A 记录，曾导致排查很久）。以后加子域名（如 `api.`）注意同样问题。
4. **腾讯云云镜黑名单**：任何"GitHub/Azure/境外云 IP → 服务器入站"的方案都会被静默丢弃。想改部署方式时记住这点；出站（服务器→GitHub）不受影响。
5. **服务器实为 2 核 3.5G**：用户以为租了 4C4G，实际规格是 2C4G 左右。开发阶段够用，AI worker + 大并发时可能吃紧，可提醒用户向腾讯云确认。
6. **.env 不入库**：`deploy/.env`（MySQL 密码）只在服务器 `/opt/channel/deploy/.env`，本地只有 `.env.example`。
7. **旧服务器 49.232.252.213 有其他项目**（greendoc/Sound-Bridge/PatPlayer/QQ），内存已占用 1.5G+，不要在上面部署课设项目，也不要乱动它的服务。
8. GitHub 仓库里有两个无用 Secrets（SERVER_HOST / SERVER_SSH_KEY），删不删都行。
9. 数据库迁移用 Alembic（backend 里尚未初始化），**禁止手改生产库**（方案文档要求）。
10. `backend/.env`、`frontend/.env` 等敏感文件已在 .gitignore 中，新增配置时注意别提交密钥。

---

## 9. 下一步计划（按课设方案 8~10 周）

| 优先级 | 事项 |
|---|---|
| 🔴 现在 | 开发**账号系统**：用户表（SQLAlchemy 模型）+ Alembic 迁移 + 注册/登录/JWT（bcrypt） |
| 🔴 紧接着 | 频道/版块 CRUD（复用调研的原生字段设计，见 `research_cli_mcp_native_fields.md`） |
| 🟡 第 3~4 周 | 发帖（富文本分片）/评论楼中楼/点赞/关注/帖子流 |
| 🟡 第 5 周 | 权限（身份组 RBAC）/管理动作/搜索 |
| 🟢 第 6 周 | WebSocket 通知 + 分享短链 + Feed 排序 |
| 🟢 第 7~8 周 | AI 一期（帮写/巡检审核/问答机器人 RAG）+ 看板 |
| 🟢 第 9~10 周 | Android App（Kotlin Compose）+ 答辩文档 |

**开发节奏建议**：每完成一个功能就 `git push`，3 分钟内自动上线，可在 https://guild.weaxi.cn 上实时验收——这就是本会话搭好链路的意义。

---

## 10. 交接检查清单（新会话先验证这 6 条）

- [ ] `ssh_exec` 122.152.219.236 能连通
- [ ] `curl -sk https://guild.weaxi.cn/healthz` 返回 ok
- [ ] `curl https://api.github.com/repos/Dc-D666/dbs-curdeg`（或 git ls-remote）确认仓库可达
- [ ] 服务器 `crontab -l` 里有 auto-deploy 条目、`crond` active
- [ ] 本地 `git push` 能成功（代理 7897 开着）
- [ ] `/opt/channel` 代码与 GitHub main 一致（`git -C /opt/channel rev-parse HEAD` == 本地 HEAD）
