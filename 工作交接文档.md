# 工作交接文档 —— dbs-curdeg（仿「腾讯频道」课设项目）

> 生成时间：2026-08-19 ｜ 用途：会话切换后，新会话无需重新摸索即可接手
> 本机工作目录：`D:\DBS-curdeg` ｜ GitHub：https://github.com/Dc-D666/dbs-curdeg（私有）

---

## 0. 一句话总结

一个「仿腾讯频道」大学生课设项目（Web + Android 双端，共用 FastAPI API），**开发→部署全链路已经打通**：本地 `git push` 到 GitHub 私有仓库，服务器每 3 分钟自动拉取并重建（也可 ssh 免密秒级手动触发），站点跑在 **https://guild.weaxi.cn**（HTTPS 已配好自动续期）。

**进度：阶段 1（账号）/ 2（频道）/ 3（内容互动 + 富文本全分片）/ 4（身份组 RBAC + 管理动作 + 搜索）全部完成上线**，前端已按 TDesign 设计体系重构（tokens/底部 Tab/组件库），74 个后端测试全绿。下一步是**阶段 5（WebSocket 通知 + 分享短链 + Feed 热度）**。

---

## 1. 项目定位

- **做什么**：模仿腾讯频道（QQ 频道）私域平台——频道→版块→帖子→评论/点赞→身份组→治理→AI 功能
- **技术栈**（已在 `课设技术栈推荐与实施方案.md` 定稿）：
  - 后端：Python 3.12 + FastAPI + SQLAlchemy + MySQL 5.7 + Redis 7 + WebSocket
  - 前端：Vue3 + Vite（移动优先 H5 + PC 管理后台）
  - Android：Kotlin + Jetpack Compose（**尚未开始**）
  - AI：DeepSeek（LLM 网关、AI 帮写、内容巡检、RAG 问答机器人）
- **调研资料**（在仓库里）：`research_tencent_channel_public.md`、`research_cli_mcp_native_fields.md`、`课设技术栈推荐与实施方案.md`、`开发部署工作流方案.md`（注意：后一份文档的部署章节仍是旧方案，见 §8）

---

## 2. 当前已上线的东西（验收点）

| 项目 | 状态 |
|---|---|
| 线上站点 | **https://guild.weaxi.cn**（HTTP 自动 301 跳转 HTTPS） |
| 健康检查 | `https://guild.weaxi.cn/healthz` → `{"status":"ok",...}` |
| API 测试 | `https://guild.weaxi.cn/api/v1/ping` → `{"message":"pong"}` |
| 账号系统 | 注册（邮箱验证码）/ 登录 / JWT / 资料 / 改密，字段校验失败返回友好中文提示 |
| 频道系统 | 频道/版块/成员/加入审核 CRUD + 图片上传，前端发现/详情/管理页（建版块/成员/审核/解散） |
| 内容系统 | 发帖 / 评论楼中楼 / 点赞幂等 / 关注 / 帖子流（频道最新·热门 / 全站 / 关注流） |
| 富文本编辑器 | 自研 RichEditor：文本/链接/图片 + **@提及（仅成员）/ 话题 / emoji / 行内样式（粗斜删·颜色·背景·字号）** + markdown 快捷语法（`**粗**` `*斜*` `` `码` `` `~~删~~`） |
| 权限体系（阶段4） | 成员→身份组(roles.perms)→权限点 RBAC；`core/deps.require_perms()` 依赖工厂；owner/系统管理员恒全量，member_type 兜底兼容老数据；越级防护（level 权重比较） |
| 管理动作（阶段4） | 置顶/加精/删他人帖/删评论/审核加入 全走权限点；禁言（到期自动解除）/解除/踢出/拉黑/解除拉黑/成员身份分配；**所有管理动作 op_logs 留痕**，`GET /communities/{cid}/ops` 可查 |
| 搜索（阶段4） | `GET /search/posts` 关键词搜索：生产 FULLTEXT ngram 索引（迁移建）+ 测试/降级 LIKE 兜底双路召回、标题/摘要 `<em>` 高亮、可选频道过滤、搜索记录落库；`GET /search/hot` 近 7 天热门词 TOP10 |
| 设计体系 | TDesign tokens（品牌色 #0052d9）/ 底部 Tab 导航 / FeedCard / Toast / 骨架屏 / 空状态 / 头像组件 |
| HTTPS 证书 | Let's Encrypt（certbot），有效期至 2026-11-17，自动续期已配置 |

---

## 3. 关键信息速查表

| 项 | 值 |
|---|---|
| 新服务器（课设用） | **122.152.219.236**（root，OpenCloudOS 9.6，实为 2 核 / 3.5G 内存——用户以为租的是 4C4G，值得再确认） |
| 旧服务器（其他项目） | 49.232.252.213（greendoc / Sound-Bridge / PatPlayer / QQ 等旧项目，勿动） |
| 域名 | `guild.weaxi.cn` → A 记录 `122.152.219.236`（DNSPod，注意：记录类型必须选 **A**，选"显性URL"不生效） |
| GitHub 仓库 | `Dc-D666/dbs-curdeg`（私有） |
| 本地 git 凭证 | HTTPS + Windows 凭证管理器（Git Credential Manager） |
| SSH 主机配置 | dsh-ssh 插件已配好两台服务器（侧边栏「SSH」入口） |
| SSH 免密（自动化用） | 本机 `C:\Users\33031\.ssh\id_ed25519_deploy`（无 passphrase），公钥已装服务器 `/root/.ssh/authorized_keys` |
| 数据库（本地测试） | 库 `guild` / 用户 `guild` / 密码见 `backend/.env`（gitignore 不入库）；测试库 `guild_test` 已在服务器建好并授权 |
| SSH 隧道（本地连服务器库） | 守护脚本 `_tmp_tunnel.py`（断开自动重连）：13306→MySQL、16379→Redis；`backend/.env` 已指向 |
| push 推荐命令 | `git -c http.proxy= -c http.sslBackend=openssl push origin main`（三通道中最稳） |

---

## 4. 部署架构（最终版，重要）

```
本地 git push → GitHub（私有仓库）
                    ↑
部署触发（auto-deploy.sh 已版本化进仓库 deploy/auto-deploy.sh）：
  ├─ 手动（推荐，秒级）：ssh 免密 → bash /opt/channel/deploy/auto-deploy.sh
  ├─ cron 兜底：每 3 分钟轮询
  └─ 逻辑：git fetch → 无变化退出；有变化才 reset --hard
     → docker compose up -d --build → 健康检查(https://127.0.0.1/healthz, 60s)
     → 构建失败或健康检查超时自动回滚上一提交
  └─ 日志：/var/log/auto-deploy.log
```

**手动触发（免等 cron，部署调试推荐）**：
```bash
ssh -i C:\Users\33031\.ssh\id_ed25519_deploy root@122.152.219.236 "bash /opt/channel/deploy/auto-deploy.sh"
```

**为什么不用 GitHub Actions 直接部署（重要背景）**：腾讯云安全组件「云镜 YunJing」的全球黑名单（iptables INPUT 第 1 条 DROP 规则，ipset `YJ-GLOBAL-INBLOCK` 含 1 万+ IP，含 GitHub Actions 的 Azure 段）会**静默丢弃 GitHub 运行器的入站连接**，Actions 的 SSH 永远连不上服务器（5 次运行全部失败已验证）。所以改成**服务器主动出站拉取**，绕开入站依赖。GitHub Actions 现在只做纯测试（`.github/workflows/deploy.yml`，无 SSH）。

**容器编排**（`deploy/docker-compose.yml`，在 `/opt/channel/deploy/` 下执行）：
- `web`：nginx（前端静态 + 反代 `/api/`、`/healthz`、`/ws`、`/uploads/`），对外 80/443
- `api`：FastAPI（`backend/` 构建）
- `mysql`：5.7（卷 `mysql_data`，buffer pool 384M）
- `redis`：7-alpine（卷 `redis_data`）
- 共享卷 `uploads`（用户上传文件，nginx 直接服务）

**HTTPS 机制**：
- 证书：`/etc/letsencrypt/live/guild.weaxi.cn/`（certbot，webroot 模式，webroot 卷 `/var/www/certbot`）
- nginx 读的是 `/opt/channel/deploy/certs/`（容器内挂载为 `/etc/nginx/cert`），**续期挂钩**（renewal conf 的 `renew_hook`）会自动 cp 证书 + `docker exec deploy-web-1 nginx -s reload`
- `certbot renew --dry-run` 已验证通过

---

## 5. 服务器明细（122.152.219.236）

**软件**：Docker 29.7.2 + Compose v5.5.0 + git + certbot 2.8.0 + crond（已运行）

**Docker 国内加速**（`/etc/docker/daemon.json`）：
```json
{ "registry-mirrors": ["https://mirror.ccs.tencentyun.com", "https://docker.m.daocloud.io"] }
```
（拉镜像 80s → 10s；后端 Dockerfile 用清华 pip 源、前端用 npmmirror，都是为国内构建）

**密钥/凭据（都在服务器上）**：
- `/root/.ssh/github_deploy`：**部署密钥**（read-only），公钥已加到 GitHub 仓库 Deploy Keys，用于服务器拉代码——**这是部署链路的核心，别删**
- `/root/.ssh/ci_key`：CI 密钥（公钥在 authorized_keys），原为 GitHub Actions SSH 用，**现在已无用**，可删可留
- `/opt/channel/deploy/.env`：生产环境变量（含随机生成的 `MYSQL_ROOT_PASSWORD`，**不入库**，别泄露别删）
- `/root/.ssh/config`：github.com 走 `github_deploy` 密钥

**crontab**（root）：
```
*/3 * * * * /opt/channel/deploy/auto-deploy.sh >> /var/log/auto-deploy.log 2>&1
```
（另有腾讯云 stargate 自带条目，勿动）

**防火墙**：22/80/443 开放；iptables 第 1 条为云镜黑名单 DROP（见 §4）

---

## 6. 代码仓库与本地环境

**仓库结构**（全部已推送）：
```
D:\DBS-curdeg\
├── backend/            # FastAPI：api/v1 13 路由（auth/users/communities/boards/members/uploads/posts/comments/interact/topics/roles/manage/search）+ 12 模型 + services + alembic（6 迁移）
├── frontend/           # Vue3：views 11 页 + components（FeedCard/RichEditor/SkeletonFeed/EmptyState/UserAvatar）+ TDesign 图标 + tokens
├── deploy/             # docker-compose.yml、deploy.sh（选择性重建 api/web）、auto-deploy.sh（健康检查+回滚）、.env.example
├── .github/workflows/  # 纯测试 CI（后端冒烟 + 前端 typecheck/build）
└── 各 .md 方案/调研文档
```

**git 提交历史**（40+ 个，全部在 main；业务阶段从 `35bdf7f` 开始，最新 10 个）：
```
79351aa fix: 移动端底部导航 /me 页变宽溢出修复（overflow-x/tabbar min-width）
a9674d4 fix: theme-color 对齐 TDesign 0052d9
456d857 feat: 富文本完整分片 — @提及(仅成员)/话题(topics表)/emoji/行内样式+markdown语法
ab37279 feat: 富文本编辑器（阶段3收尾，RichEditor contenteditable 分片序列化）
1f3329b feat: TDesign Starter 组件层 — Toast/骨架屏/空状态/UserAvatar
2a02ba1 feat: TDesign 设计系统 — tokens/底部Tab/FeedCard/首页全站帖子流/GET /feed
5c26920 fix: 创建频道自动建默认版块（否则新频道无法发帖）
a15335d feat: 页面连通性修复 — owner管理面板/编辑帖子/改密码/关注流页/他人主页/404/redirect
7b19d68 fix: 代码审查修复 — 频道状态校验/原子计数/级联删回复/feed去重/批量组装
9a94425 feat: 阶段3内容系统 — 发帖/帖子流/评论楼中楼/点赞/关注，42测试
bf50bc2 fix: auto-deploy.sh 漏 git reset --hard（部署假成功根因）
1a28c20 fix: 部署脚本执行位（cron 直接执行需要 +x）
18d1817 docs: 交接文档更新
beeae25 fix: 注册校验友好化 + 部署链路优化（CI前端检查/选择性重建/自动回滚）
（更早见 git log）
```

**本机 git 配置（已持久化）**：
```
http.sslBackend = schannel        # 直连被墙时用 schannel 能通
http.version    = HTTP/1.1
http.proxy      = http://127.0.0.1:7897   # Clash 代理（保留备用）
```
> ⚠️ **推送网络（重要）**：本机网络极不稳定，GitHub 三通道**交替可用**，push 失败换通道重试即可（不是代码问题）：
> 1. Clash 代理：`git push`（默认走 7897）
> 2. VPN 直连：`git -c http.proxy= push`
> 3. 直连+openssl（最稳）：`git -c http.proxy= -c http.sslBackend=openssl push origin main`

**dsh-ssh 插件**：已配两台服务器（密码认证），新会话可直接 `ssh_exec`/`ssh_upload` 操作 122.152.219.236。

---

## 7. 常用操作命令

```bash
# 本地开发（热更新）
cd D:\DBS-curdeg\backend && pip install -r requirements.txt && uvicorn app.main:app --reload
cd D:\DBS-curdeg\frontend && npm install && npm run dev

# push（网络不稳时最稳的通道）
git -c http.proxy= -c http.sslBackend=openssl push origin main

# 本地集成测试（经 SSH 隧道连服务器 MySQL/Redis，测试库 guild_test 自动建表+事务回滚）
# 1) 先起守护隧道（run_background 常驻，断开自动重连）：
python _tmp_tunnel.py
# 2) 全量测试（backend/ 目录下）：
cd backend && python -m pytest -q   # 当前 53 用例全绿

# 线上回归（绕开本机不稳定网络，在服务器 api 容器内跑）：
# 模式：本地写 _tmp_*.py → paramiko sftp 传 /tmp → docker cp 进 deploy-api-1 → docker exec python 执行
# （容器内 BASE=http://127.0.0.1:8000/api/v1，验证码直接写 redis）

# 部署（手动触发，免等 cron；推荐）：
ssh -i C:\Users\33031\.ssh\id_ed25519_deploy root@122.152.219.236 "bash /opt/channel/deploy/auto-deploy.sh"
tail -20 /var/log/auto-deploy.log          # 看部署日志
docker compose -f /opt/channel/deploy/docker-compose.yml ps   # 看容器

# 选择性重建（只改一端时）：
ssh -i C:\Users\33031\.ssh\id_ed25519_deploy root@122.152.219.236 "cd /opt/channel && bash deploy/deploy.sh api"  # 或 web

# 证书
certbot renew --dry-run                     # 模拟续期（已配 renew_hook 自动同步+reload）

# 验证
curl -sk https://guild.weaxi.cn/healthz
```

---

## 8. 已知问题与注意事项（新会话必读）

1. **`开发部署工作流方案.md` 的部署章节写的是旧方案**（GitHub Actions SSH 部署），与实际落地（服务器 cron）不符，有空应更新，避免误导。
2. **推送网络**：本机网络极不稳定，GitHub 三通道**交替可用**（Clash 7897 / VPN 直连 `-c http.proxy=` / 直连+openssl `-c http.proxy= -c http.sslBackend=openssl`，后者最稳）。push 失败换通道重试即可，不是代码问题。可用 `urllib urlopen('https://github.com')` 快速测网络。
3. **DNS 记录类型**：`guild.weaxi.cn` 必须用 **A 记录**（DNSPod 的「显性URL」选项不会产生真实 A 记录，曾导致排查很久）。以后加子域名（如 `api.`）注意同样问题。
4. **腾讯云云镜黑名单**：任何"GitHub/Azure/境外云 IP → 服务器入站"的方案都会被静默丢弃。想改部署方式时记住这点；出站（服务器→GitHub）不受影响。
5. **服务器实为 2 核 3.5G**：用户以为租了 4C4G，实际规格是 2C4G 左右。开发阶段够用，AI worker + 大并发时可能吃紧，可提醒用户向腾讯云确认。
6. **.env 不入库**：`deploy/.env`（MySQL 密码、SMTP 授权码等）只在服务器 `/opt/channel/deploy/.env`，本地只有 `.env.example`。
7. **旧服务器 49.232.252.213 有其他项目**（greendoc/Sound-Bridge/PatPlayer/QQ），内存已占用 1.5G+，不要在上面部署课设项目，也不要乱动它的服务。
8. GitHub 仓库里有两个无用 Secrets（SERVER_HOST / SERVER_SSH_KEY），删不删都行。
9. 数据库迁移用 Alembic（backend/alembic/versions 已有 6 个迁移，容器启动自动执行），**禁止手改生产库**（方案文档要求）。
10. `backend/.env`、`frontend/.env` 等敏感文件已在 .gitignore 中，新增配置时注意别提交密钥。
11. **自动化 ssh 用专用密钥** `id_ed25519_deploy`（无 passphrase）；本机原 `id_ed25519` 私钥带 passphrase，非交互场景不可用。服务器 authorized_keys 里有一把旧的 `github-actions-ci` 公钥（对应 /root/.ssh/ci_key），已无用可清理。
12. 服务器密码只出现在一次性临时脚本（_tmp_*.py）中，用完即删，不入库不入记忆。
13. **auto-deploy.sh 曾有"部署假成功"bug**（bf50bc2 修复）：旧版漏 `git reset --hard`，fetch 后直接 build 旧工作区，日志显示成功但线上不变。教训：部署后确认镜像 digest 变化（`docker images deploy-api` 时间戳）或容器 Recreate。
14. **部署脚本执行位**：auto-deploy.sh / deploy.sh 已 `git update-index --chmod=+x` 固化（cron 直接执行需要 +x，git reset 会重置权限所以必须入库）。
15. **PWA 缓存**：前端更新后用户浏览器可能一直显示旧版（service worker 缓存），验收/体验前用**无痕窗口或强刷**（Ctrl+Shift+R）——用户多次"看不到新功能"都是缓存问题。
16. **SSH 隧道频繁断**：ssh 无内建重连，用 `_tmp_tunnel.py` 守护（自动重连+端口释放等待+ExitOnForwardFailure）。端口 13306/16379（3307/6380 被沙箱拒绑）。
17. **npm 包名**：tdesign 图标库是 `tdesign-icons-vue-next`（**无 @scope**）；`@tdesign-icons-vue-next` 会报 EINVALIDTAGNAME。
18. **编辑教训**：multi_edit 的 search/replace 末尾换行不一致会吞换行导致 SyntaxError（发生过 2 次），改完必跑 pytest/typecheck。
19. **线上演示数据**：有验收频道/回归频道等测试数据（含 super 用户、e2e 账号），可留作演示或删除。
20. **未提交改动**：`课设技术栈推荐与实施方案.md`（300+ 行）和 `designer/` 目录是用户自己的未提交改动，提交时不要 `git add -A` 误带。

---

## 9. 下一步计划（按课设方案 8~10 周）

| 优先级 | 事项 |
|---|---|
| ✅ 已完成 | 阶段 1 账号 / 阶段 2 频道 / 阶段 3 内容互动 + 富文本全分片 + 阶段 4 身份组 RBAC + 管理动作 + 搜索 + TDesign 设计系统 + 部署链路优化（74 测试全绿） |
| 🔴 现在（阶段 5） | **WebSocket 通知 + 分享短链 + Feed 热度**（方案 741-770 行）：notification/short_link/feed_strategy 表；ws/manager + ws/events 推送；notifications 列表/已读；短链生成解析跳转计数；热度 score=like*1+comment*2+favorite*3+时间衰减（Redis 缓存）；前端通知中心 + 角标 + 分享按钮 |
| 🟡 阶段 5 | WebSocket 通知 + 分享短链 + Feed 热度（score=like*1+comment*2+favorite*3+时间衰减，Redis 缓存） |
| 🟢 阶段 6 | AI 一期：LLM 网关（GLM 主 + DeepSeek 兜底，config 已预留）/ 帮写（SSE）/ 巡检审核 / RAG 问答 |
| 🟢 阶段 7~8 | 运营看板 + 搜索增强 + 通知完善 |
| 🟢 阶段 9~10 | Android App（Kotlin Compose，届时用 Android Studio）+ 答辩文档 |

**阶段 4 验收标准**（已全部达成并测试覆盖）：管理员能置顶/删他人帖、普通成员不能（1002）；禁言到期自动解除；被踢/拉黑成员无法再进频道发帖；中文关键词能搜到帖子并带高亮词；管理后台所有操作 op_logs 留痕。

**阶段 4 遗留说明**：语义 embedding 召回（双路融合）未实现——依赖阶段 6 的 GLM Embedding key，`search_service._semantic_recall()` 挂点已预留；FULLTEXT ngram 索引只在生产迁移建，测试库走 LIKE 兜底路径（行为一致，已有测试覆盖）。

**开发节奏建议**：每完成一个功能就 `git push`（用推荐命令），随后 ssh 免密触发部署，线上实时验收。

---

## 10. 交接检查清单（新会话先验证）

- [ ] 免密 ssh 能连通：`ssh -i C:\Users\33031\.ssh\id_ed25519_deploy -o BatchMode=yes root@122.152.219.236 echo ok`
- [ ] `curl -sk https://guild.weaxi.cn/healthz` 返回 ok
- [ ] `curl https://api.github.com/repos/Dc-D666/dbs-curdeg`（或 git ls-remote）确认仓库可达
- [ ] 服务器 `crontab -l` 里有 auto-deploy 条目、`crond` active
- [ ] 本地 `git push` 能成功（三通道：Clash 7897 / VPN 直连 / 直连+openssl）
- [ ] `/opt/channel` 代码与 GitHub main 一致（`git -C /opt/channel rev-parse HEAD` == 本地 HEAD）
- [ ] 本地集成测试可用：起 `_tmp_tunnel.py` 守护隧道 → `cd backend && python -m pytest -q` 全绿（74）
- [ ] `git log --oneline -3` 最新提交 == 4a7fb7b（阶段 4 完成提交）

---

## 11. 本 session 工作回顾（2026-08-19 ~ 08-20，遇到的问题与解决）

| 问题 | 解决方法 | 提交 |
|---|---|---|
| 注册报笼统"参数错误"（字段校验失败信息被吞） | validation handler 输出友好中文提示 + 前端提交前本地校验 | beeae25 |
| 部署每次等 cron 轮询（0~3 分钟） | ssh 免密手动触发 auto-deploy.sh；cron 兜底 | beeae25 |
| 免密 ssh 失败（原私钥带 passphrase） | 生成专用密钥 `id_ed25519_deploy`（无密码）+ paramiko 诊断 | — |
| auto-deploy "部署成功"但线上不变（假成功） | 缺 `git reset --hard`——fetch 后构建的是旧工作区；修复+部署后查镜像 digest | bf50bc2 |
| cron 连续 Permission denied | 版本化脚本无执行位：chmod +x + `git update-index --chmod=+x` 固化 | 1a28c20 |
| 创建频道后无法发帖（无版块） | 创建频道自动建"默认版块"；旧频道在管理面板手动建 | 5c26920 |
| 首页/登录页/发帖页找不到入口 | 游客导航加"发现"、首页"浏览频道"按钮；后续 9 处连通性修复（owner 管理面板/编辑帖子/改密码/关注流页/他人主页/404/redirect） | 7d9ba46, a15335d |
| 代码审查（review 子代理）发现权限/并发缺陷 | 写路径统一校验频道状态 + 点赞需成员 + SQL 原子计数 + 级联删回复 + feed 去重 + 批量组装（N+1→6 查询） | 7b19d68 |
| SSH 隧道频繁断（ssh 无重连） | `_tmp_tunnel.py` 守护脚本：断线 5s 自动重连 + 端口释放等待 | — |
| 富文本只有 textarea | RichEditor v2：contenteditable + 4.4 分片序列化（文本/链接/图片→@/话题/emoji/样式 + markdown 快捷语法），后端 rich_content 优先 + source_markdown 提取 + @ 仅成员校验 + topics 表 | ab37279, 456d857 |
| 移动端底部导航在 /me 变宽溢出 | 全局 overflow-x 兜底 + tabbar max-width/min-width:0 + svg 强制尺寸 + MeView flex 防护 | 79351aa |
| TDesign 图标 npm 包名报错 | 正确包名 `tdesign-icons-vue-next`（无 @scope，v0.4.10） | 2a02ba1 |
| 本机网络极不稳定（push 三通道交替失败） | 轮换：Clash 7897 / VPN 直连 / 直连+openssl；urllib 走系统代理可测通 | — |

**后端接口全量清单**（50+ 端点）：auth（注册/登录/刷新/登出/改密/验证码）、users（me/资料/头像/他人主页）、communities（CRUD/状态/加入/退出）、boards（CRUD）、members（列表/加入审核）、uploads、posts（发帖/编辑/删除/置顶/精华/详情）、feed（频道/全站/关注流，latest+hot keyset 游标）、comments（顶层/楼中楼/删除）、likes/follows（幂等）、topics（列表/upsert）、**roles（阶段4：身份组列表/创建/更新/删除/成员身份分配）**、**manage（阶段4：禁言/解除禁言/踢出/拉黑/解除拉黑/操作日志 GET /ops）**、**search（阶段4：/search/posts 关键词+高亮、/search/hot 热门词）**。

**前端页面**（12 个）：Home（全站帖子流）/ Discover（搜索框+热门词+结果高亮）/ CommunityDetail（帖子流+owner 管理）/ **Admin（阶段4：/c/:id/admin 成员管理·身份组配置·操作日志）** / PostCreate（RichEditor）/ PostDetail（分片渲染+评论楼中楼）/ MyFeed（关注流）/ Me（资料+改密）/ UserProfile / Login / Register / NotFound，底部 Tab 导航（首页/发现/我的）。
