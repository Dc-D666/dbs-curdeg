# 数据库优化验收指南

> 验收库 `guild_accept`：生产备份真实数据 + 全部优化后的新结构（v2 结构）。
> 与生产库 `guild` 完全隔离，随便看、随便跑、随便演示，不会影响线上。

---

## 一、连接方式（两步）

### 1. 开 SSH 隧道（本地 CMD/PowerShell 窗口，保持开着）

```powershell
ssh -i C:\Users\33031\.ssh\id_ed25519_deploy -N -L 13306:127.0.0.1:3306 root@122.152.219.236
```

### 2. 用 Navicat / DataGrip / DBeaver 连接

| 参数 | 值 |
|---|---|
| 主机 | 127.0.0.1 |
| 端口 | 13306 |
| 用户 | guild |
| 密码 | 同 `backend/.env` 里的 `MYSQL_PASSWORD` |
| 默认库 | **guild_accept** ← 全部验收在这里 |

> ⚠️ 不要连 `guild`（那是生产库）。验收库名带 `_accept` 后缀。

---

## 二、验收点清单

### A. 结构验收：31 张表 + 数据库对象（对象树直接看）

连接后展开 `guild_accept`，应看到：

**A1. 表 = 31 张**（30 张 ORM 模型表 + 1 张台账表 counter_audit，另有 alembic_version 为迁移工具表、6 个视图）

重点看这 5 张**新拆分/新增的表**（本次优化的核心动作）：

| 表 | 来源 | 验收看点 |
|---|---|---|
| `post_likes` | 原 likes 拆分 | 外键 fk_post_likes_post / fk_post_likes_user，UNIQUE(post_id, user_id) |
| `comment_likes` | 原 likes 拆分 | 外键 fk_comment_likes_comment / fk_comment_likes_user |
| `post_contents` | posts 垂直拆分 | 1:1 主键即外键 post_id；source_markdown/rich_content/images |
| `board_role_perms` | 原 boards JSON 列规范化 | 复合主键 (board_id, role_id) 即两个外键 |
| `post_embeddings` | 原 posts.embedding JSON 列 | AI 向量独立表 |
| `counter_audit` | 新增台账表 | 触发器写入的计数器变更证据链 |

原 `likes` 表已不存在（多态 0 哨兵设计清除）；`posts` 表里已无 rich_content/source_markdown/images/embedding 四个大列（垂直拆分）。

**A2. 外键 = 45 个**。跑一句：

```sql
SELECT TABLE_NAME, CONSTRAINT_NAME, COLUMN_NAME, REFERENCED_TABLE_NAME
FROM information_schema.KEY_COLUMN_USAGE
WHERE TABLE_SCHEMA = 'guild_accept' AND REFERENCED_TABLE_NAME IS NOT NULL
ORDER BY TABLE_NAME;
```

级联策略抽查（看 DELETE_RULE）：

```sql
SELECT TABLE_NAME, CONSTRAINT_NAME, DELETE_RULE
FROM information_schema.REFERENTIAL_CONSTRAINTS
WHERE CONSTRAINT_SCHEMA = 'guild_accept'
ORDER BY DELETE_RULE, TABLE_NAME;
```

预期三类：CASCADE（从属数据，如帖子删→评论删）、SET NULL（弱引用，如话题删→帖子 topic_id 置空）、RESTRICT（作者类强引用，如 posts.author_id → users.id）。

**A3. 视图 ×6**：v_post_card / v_hot_posts / v_member_profile / v_community_overview / v_pending_reports / v_ai_degraded_calls。
直接 `SELECT * FROM v_post_card LIMIT 5;` —— 一条 SQL 拿到帖子卡片全部展示字段（联表已在视图内）。

**A4. 触发器 ×12 / 存储过程 ×1 / 函数 ×1 / 事件 ×4**：

```sql
SHOW TRIGGERS;                 -- 12 个，全部是 trg_*_ai/au/ad 对账型触发器
SHOW PROCEDURE STATUS WHERE Db='guild_accept';   -- sp_reconcile_counters
SHOW FUNCTION STATUS WHERE Db='guild_accept';    -- fn_post_heat
SHOW EVENTS;                   -- 4 个（含新加的 ev_clean_notifications）
```

**A5. 索引**（复合索引以真实查询形态设计）：

```sql
SHOW INDEX FROM posts;   -- 看 ix_posts_community_status_top_id 等 keyset 复合索引
SHOW INDEX FROM post_contents;   -- ft_post_content 全文索引（ngram）
```

### B. 数据搬家验收：真实数据零丢失

```sql
SELECT
  (SELECT COUNT(*) FROM posts)                       AS 帖子,
  (SELECT COUNT(*) FROM post_contents)               AS 正文(应=帖子),
  (SELECT (SELECT COUNT(*) FROM post_likes)
        + (SELECT COUNT(*) FROM comment_likes))      AS 点赞(应=710),
  (SELECT COUNT(*) FROM users)                       AS 用户,
  (SELECT COUNT(*) FROM comments)                    AS 评论,
  (SELECT COUNT(*) FROM boards)                      AS 版块,
  (SELECT COUNT(*) FROM notifications)               AS 通知;
```

预期：113 / 113 / 710 / 37 / 214 / 60 / 2。原来 `likes` 的 710 行全部按"帖子赞/评论赞"正确分流（该备份里全部是帖子赞，故 post_likes=710）。

### C. 机制演示（验收库上随便跑，最有讲解价值）

**C1. 触发器台账演示**——点赞一行，看台账自动记账：

```sql
-- 模拟一次点赞（随便挑一帖一用户）
INSERT INTO post_likes (post_id, user_id)
SELECT p.id, u.id FROM posts p JOIN users u ON u.id = 1
WHERE p.id = (SELECT MIN(id) FROM posts)
  AND NOT EXISTS (SELECT 1 FROM post_likes WHERE post_id = p.id AND user_id = 1);

-- 台账立刻多了一行 delta=+1 的记录：
SELECT * FROM counter_audit ORDER BY id DESC LIMIT 3;

-- 清理演示数据（级联会顺带再记 -1 台账，这就是闭环）
DELETE FROM post_likes WHERE post_id = (SELECT MIN(id) FROM posts) AND user_id = 1;
SELECT * FROM counter_audit ORDER BY id DESC LIMIT 1;   -- delta=-1
```

**C2. 对账存储过程幂等演示**——跑前后计数不变 = 应用层计数零漂移：

```sql
SELECT id, like_count, comment_count, favorite_count FROM posts WHERE id = 1;
CALL sp_reconcile_counters();
SELECT id, like_count, comment_count, favorite_count FROM posts WHERE id = 1;  -- 应不变
```

**C3. 中文全文检索**（ngram 分词，搜"山大"或"考研"）：

```sql
SELECT id, title FROM posts
WHERE MATCH(title) AGAINST ('考研' IN NATURAL LANGUAGE MODE) LIMIT 5;

SELECT post_id, LEFT(source_markdown, 50) FROM post_contents
WHERE MATCH(source_markdown) AGAINST ('考研' IN NATURAL LANGUAGE MODE) LIMIT 5;
```

**C4. 视图即查即用**：

```sql
SELECT * FROM v_community_overview;   -- 频道成员数/帖数 与源表实算值并排（对账可视化）
SELECT * FROM v_ai_degraded_calls;    -- AI 降级调用留痕
```

**C5. 热度函数**：

```sql
SELECT id, title, fn_post_heat(id) AS heat FROM posts ORDER BY heat DESC LIMIT 5;
```

### D.（可选）功能层验收：跑在新结构上的真实系统

如果你想在浏览器/Swagger 里点真实功能（点赞→post_likes 出行、发帖→post_contents 出行、管理端关 AI 开关→接口 403、被拒加入后重新申请），跟我说一声，我把当前代码起一个验收后端实例（连 guild_accept，独立端口，不碰生产容器），10 分钟搭好。

---

## 三、验收对照表：上次优化项 → 验收位置

| 优化项 | 验收点 |
|---|---|
| likes 多态表拆分 | A1 表结构 + B 数据 710 分流 |
| posts 垂直拆分 | A1 表结构（posts 瘦身）+ post_contents 113 行 |
| 版块白名单规范化 | A1 board_role_perms（该备份 JSON 全空，故 0 行是正确结果） |
| 向量拆表 | A1 post_embeddings |
| 45 外键全覆盖 | A2 查询结果 |
| 6 视图 | A3 |
| 12 对账触发器 + 台账 | A4 + C1 演示 |
| 对账存储过程/事件/备份脚本 | A4 + C2 演示 + `deploy/backup.sh` 代码 |
| ngram 全文索引双路 | A5 + C3 演示 |
| 热度函数 | C5 |
| 应用层原子计数修复 | C2 幂等演示（计数不漂移的机制保障） |
