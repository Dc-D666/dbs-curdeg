# 模仿「腾讯频道」私域平台 —— 技术栈推荐与课设实施方案

> 面向：大学生课设｜服务器：轻量云 2C2G（可升 4C4G）｜入口：Web + 独立 Android App（已装 Android Studio）
> 前提：你有 DeepSeek 等模型 API Key，可"疯狂"融入 AI
> 依据：`research\_tencent\_channel\_public.md`（公开调研）+ `research\_cli\_mcp\_native\_fields.md`（CLI/MCP 原生字段实测）

\---

## 一、总体判断（先给结论）

1. **必须做单体（Monolith）**，不要微服务。2G 内存跑微服务全家桶会很难受，单体 + 模块化分层（auth/community/content/interact/ai/notification）完全够，且课设答辩更好讲。
2. **前后端分离 + 一套 REST/WebSocket API 双端复用**：Web（Vue3）和 Android（Kotlin）消费同一套 API，这是"双端"成本最低的路线。
3. **AI 做成后端内的一个独立服务模块（ai/）**，通过统一 LLM 网关调用 DeepSeek（OpenAI 兼容协议），异步任务（审核/摘要/巡检）用简单任务队列，不引重型框架。
4. **课设范围裁剪**：先做核心闭环（账号→频道→版块→发帖→评论/点赞→关注→搜索→管理→通知），AI 作为亮点增量叠加，Web 与 App 同功能。

\---

## 二、推荐技术栈（主推一套）

|层|选型|理由（结合 2C2G + AI + 双端）|
|-|-|-|
|**后端**|**Python 3.12 + FastAPI + SQLAlchemy(2.x) + Alembic + Uvicorn**|① 轻量：空载 80MB 左右，2G 内存留足给 MySQL+Redis+AI worker；② AI 生态最顺（DeepSeek 官方 SDK/OpenAI 兼容、embedding、函数调用都有 Python 一等公民支持）；③ 原生 WebSocket（通知推送）；④ Pydantic 强类型正好对应调研到的"富文本分片"这类嵌套结构|
|**数据库**|**MySQL 8.0**（InnoDB，`utf8mb4`）|中文课设最稳、答辩最顺；JSON 字段存富文本分片；FULLTEXT + ngram 支持中文搜索。备选：PostgreSQL 16（JSONB + tsvector + pgvector 更强，若想上语义搜索可换）|
|**缓存/队列**|**Redis 7**|缓存热点 Feed、在线状态、点赞计数；Pub/Sub 做站内通知；也兼作简单任务队列|
|**实时通知**|**WebSocket（FastAPI 原生 / 或 socket.io）**|实现"互动消息/系统通知"实时推送；App 端用 OkHttp WebSocket 同接|
|**对象存储**|本地磁盘 + Nginx 静态托管（课设规模够用），路径存 DB；后续可换 OSS/COS|2G 服务器 + 轻量带宽，本地最省事|
|**Web 前端**|**Vue 3 + Vite + TypeScript + Pinia + Vant(移动优先) + Element Plus(管理后台) + ECharts(数据看板)**|移动优先响应式（腾讯频道 Web 本来也薄）；管理后台给频道主用|
|**Android App**|**Kotlin + Jetpack Compose + MVVM + Retrofit/OkHttp + Coil + WebSocket + DataStore**|Android Studio 直接建；单 Activity + Compose Navigation；与 Web 共用 API|
|**部署**|**Docker Compose**（nginx + api + mysql + redis + ai-worker）或 宝塔/1Panel|2G 下 Compose 一个 yaml 起全套，迁移/演示方便；升级 4C4G 后再加更多 AI worker|
|**认证**|JWT（access+refresh）+ 密码 bcrypt；预留第三方 OAuth（QQ 登录/微信登录）接口|课设演示内部账号即可，QQ 登录可作加分项|
|**监控/日志**|结构化日志 + 简单请求计数，不引 Prometheus 全家桶|控制复杂度|

> \*\*备选后端（如果你更熟这些）\*\*：Java 21 + Spring Boot 3（内存 300\~500MB，4G 建议，生态/校园最熟）；Node.js 20 + NestJS（内存 150\~250MB，TypeScript 全栈统一）。三种都能做，\*\*主推 Python 是为了把 AI 做得又深又省心\*\*。

\---

## 三、数据模型设计（直接对应实测原生字段）

调研中通过 MCP 网关拿到了腾讯频道帖子/成员/频道的原生字段（`research\_cli\_mcp\_native\_fields.md`），下面是映射到自研库表的骨架：

```
community(频道)        ── 对应 guild: id, number(频道号), name, profile, avatar\_url,
   │                          member\_count, join\_setting(自由/审核/邀请), owner\_id,
   │                          vistor\_interaction\_switch(对应原生 uint32VistorInteractionAllSwitch), created\_at
   ├─ board(版块)      ── 对应 channel: id, community\_id, name, sort, allow\_roles(JSON)
   ├─ role(身份组)     ── 对应 role: id, community\_id, name, color, perms(JSON, 见权限点)
   ├─ member(成员)     ── 对应 member: id, community\_id, user\_id, role(owner/admin/normal/robot/ai),
   │                          join\_time, shutup\_expire(禁言), gender, location(省/市 JSON)
   ├─ post(帖子)       ── 对应 feed: id, community\_id, board\_id, author\_id, title, feed\_type,
   │       │                 create\_time, is\_top, is\_essence, comment\_count, like\_count,
   │       │                 rich\_content(JSONB ← 富文本分片数组), is\_markdown, source\_markdown
   │       ├─ post\_media(媒体)  ── images: pic\_url, width,height, level\_urls(JSON, 三级响应式)
   │       ├─ post\_video        ── videos: play\_url, duration, cover\_url
   │       ├─ comment(评论/回复) ── comment: id, post\_id, parent\_id(楼中楼), user\_id, content, reply\_count
   │       └─ like(点赞)        ── post\_id/comment\_id, user\_id, created\_at (唯一约束防重复)
   ├─ topic(话题)       ── 对应 topicContent: id, community\_id, name, schema
   ├─ notification(通知) ── 对应 notices: user\_id, type(被@/被赞/新评论/系统), ref\_id, is\_read
   └─ short\_link(短链)  ── 对应 get\_share\_url: code, type(guild/feed), ref\_id
```



以下是数据库和模块方面的补充（模块和字段和操作仅供参考，不是硬性规定，如有冲突以实测原生字段为准）

\#### 一、用户与账号管理

\*\*核心字段\*\*：用户 ID、登录用户名、加密登录密码、用户昵称、头像 URL、个人简介、性别、所在地（省 / 市）、绑定邮箱、绑定手机号、账号状态（正常 / 封禁 / 注销）、账号类型（普通用户 / 系统管理员 / AI 虚拟账号）、注册时间、最后登录时间、最后登录 IP

\*\*配套操作\*\*：用户注册 / 登录、个人信息编辑、密码重置、账号封禁 / 解封、用户注销、用户信息检索



\#### 二、频道社区管理

\*\*核心字段\*\*：频道 ID、频道唯一编号、频道名称、频道简介、频道头像 URL、频道封面图、成员总数、帖子累计数、加入方式（自由加入 / 审核加入 / 邀请制）、访客互动总开关、频道所有者 ID、频道状态（正常 / 关闭 / 违规封禁）、创建时间、最后更新时间

\*\*配套操作\*\*：频道创建、频道信息编辑、频道解散、加入规则配置、频道状态调整、频道列表 / 详情查询



\#### 三、版块分区管理

\*\*核心字段\*\*：版块 ID、所属频道 ID、版块名称、版块描述、排序权重、允许发帖身份组（JSON 集合）、是否允许匿名发帖、版块状态（正常 / 隐藏 / 关闭）、创建时间、更新时间

\*\*配套操作\*\*：版块新增 / 编辑 / 删除、版块排序调整、发帖权限配置、版块列表查询



\#### 四、身份组与权限管理

\*\*核心字段\*\*：身份组 ID、所属频道 ID、身份组名称、标识颜色、等级权重、权限集合（JSON，含发帖、评论、置顶、精华、删帖、禁言、踢人、成员管理、身份组管理等权限点）、是否为系统默认组、创建时间

\*\*配套操作\*\*：身份组创建 / 编辑 / 删除、权限点配置、成员身份分配、身份组排序列表查询



\#### 五、社区成员管理

\*\*核心字段\*\*：成员记录 ID、所属频道 ID、用户 ID、频道内专属昵称、所属身份组 ID、成员类型（所有者 / 管理员 / 普通成员 / AI 机器人）、加入时间、加入渠道（主动申请 / 邀请加入）、禁言到期时间、是否拉黑、最后活跃时间

\*\*配套操作\*\*：入频道审核、成员踢出、禁言 / 解禁、身份组变更、拉黑 / 移出黑名单、成员列表 / 活跃记录查询



\#### 六、帖子主内容管理

\*\*核心字段\*\*：帖子 ID、所属频道 ID、所属版块 ID、作者 ID、帖子标题、帖子类型（普通 / 图文 / 视频 / 投票）、富文本分片内容（JSON 结构）、Markdown 源文本、是否置顶、是否精华、浏览量、评论数、点赞数、收藏数、分享数、帖子状态（正常 / 待审核 / 驳回 / 已删除）、发布时间、更新时间、最后回复时间

\*\*配套操作\*\*：帖子发布 / 编辑 / 删除、置顶 / 取消置顶、加精 / 取消精华、帖子审核、分页列表查询、详情查询



\#### 七、帖子媒体附件管理

\*\*核心字段\*\*：附件 ID、关联帖子 ID、附件类型（图片 / 视频 / 文件）、源文件 URL、缩略图 / 封面 URL、宽度像素、高度像素、文件大小、视频时长、排序序号、上传时间

\*\*配套操作\*\*：附件上传、附件删除、媒体资源关联绑定、附件列表查询



\#### 八、评论与回复管理

\*\*核心字段\*\*：评论 ID、关联帖子 ID、父评论 ID（楼中楼层级）、被回复评论 ID、评论者 ID、评论内容、评论类型、点赞数、回复数、评论状态（正常 / 待审核 / 违规删除）、发布时间、IP 属地

\*\*配套操作\*\*：发布评论、回复楼中楼、删除评论、评论点赞、评论审核、分页加载查询



\#### 九、互动行为管理

\*\*核心字段\*\*

\- 点赞记录：记录 ID、目标类型（帖子 / 评论）、目标 ID、点赞用户 ID、点赞时间

\- 收藏记录：记录 ID、目标帖子 ID、收藏用户 ID、收藏分组、收藏时间

\- 关注记录：记录 ID、关注者 ID、关注目标类型（用户 / 频道）、目标 ID、关注时间

\*\*配套操作\*\*：点赞 / 取消点赞、收藏 / 取消收藏、关注 / 取消关注、互动状态查询、个人互动列表查询



\#### 十、话题标签管理

\*\*核心字段\*\*：话题 ID、所属频道 ID、话题名称、话题描述、话题封面、话题规则、关联帖子数、热度值、创建时间

\*\*配套操作\*\*：话题创建 / 编辑 / 删除、帖子关联话题、话题热度排序、话题列表 / 详情查询



\#### 十一、内容审核与巡检管理

\*\*核心字段\*\*：审核记录 ID、内容类型（帖子 / 评论 / 用户资料）、关联内容 ID、提交用户 ID、审核状态（待审核 / 通过 / 驳回）、违规类型、违规详情、审核方式（敏感词拦截 / AI 巡检 / 人工审核）、审核人 ID、审核时间、处理结果

\*\*配套操作\*\*：敏感词库维护、AI 审核规则配置、人工审核处理、违规内容下架、审核记录查询



\#### 十二、举报申诉管理

\*\*核心字段\*\*：举报 ID、举报类型（帖子 / 评论 / 用户 / 频道）、被举报内容 ID、举报人 ID、举报原因分类、举报详情、举证附件、举报状态（待处理 / 处理中 / 已办结 / 驳回）、处理结果、处理人 ID、举报时间、办结时间

\*\*配套操作\*\*：提交举报、举报受理、违规判定与处理、结果反馈、举报记录查询统计



\#### 十三、通知消息管理

\*\*核心字段\*\*：通知 ID、接收用户 ID、通知类型（被 @、被点赞、新评论、新粉丝、系统通知、审核结果、举报反馈）、关联内容 ID、通知标题、内容摘要、是否已读、创建时间、阅读时间

\*\*配套操作\*\*：WebSocket 实时推送、消息已读标记、一键全部已读、消息列表查询、消息删除、通知开关配置



\#### 十四、分享与短链管理

\*\*核心字段\*\*：短链 ID、短链唯一编码、分享类型（频道 / 帖子 / 用户）、关联目标 ID、生成用户 ID、累计访问次数、创建时间、过期时间

\*\*配套操作\*\*：短链生成、跳转目标解析、访问量统计、短链失效、短链记录查询



\#### 十五、Feed 流与排序管理

\*\*核心字段\*\*：策略配置 ID、所属频道 ID、排序规则（最新发布 / 热度排序 / 精华优先）、热度权重配置（点赞占比、评论占比、时间衰减系数）、置顶帖权重、缓存有效期

\*\*配套操作\*\*：排序策略配置、热点 Feed 缓存更新、帖子热度计算、Feed 流分页拉取、置顶内容优先展示



\#### 十六、智能搜索管理

\*\*核心字段\*\*：搜索记录 ID、搜索用户 ID、搜索关键词、搜索范围（帖子 / 频道 / 用户 / 话题）、返回结果数、搜索时间；索引配置项：分词器类型、高亮字段、语义检索开关、向量模型参数

\*\*配套操作\*\*：关键词全文检索、语义向量召回、结果高亮展示、热门搜索词统计、搜索索引维护



\#### 十七、AI 能力配置管理

\*\*核心字段\*\*：功能 ID、功能名称（AI 帮写、内容审核、RAG 问答、内容摘要）、启用状态、调用模型、模型参数（温度、最大 Token）、Prompt 模板、调用频次上限、计费配置

\*\*配套操作\*\*：AI 功能开关、模型切换配置、Prompt 模板编辑、调用日志查询、限流规则配置



\#### 十八、系统操作日志管理

\*\*核心字段\*\*：日志 ID、操作人 ID、操作类型（新增 / 删除 / 修改 / 审核）、所属模块、操作详情、操作 IP、操作时间、请求参数、响应结果

\*\*配套操作\*\*：操作自动留痕、多条件日志查询、审计追溯、日志导出



\#### 十九、数据统计与看板管理

\*\*核心字段\*\*：统计周期（日 / 周 / 月）、所属频道 ID、新增成员数、活跃成员数、帖子发布量、互动总量、违规内容数、AI 调用次数、用户留存率

\*\*配套操作\*\*：运营数据自动统计、多维度可视化看板、数据趋势分析、报表导出



\#### 二十、系统基础配置管理

\*\*核心字段\*\*：配置键名、配置键值、配置描述、更新时间；涵盖站点名称、备案信息、上传大小限制、允许文件格式、全局敏感词开关、默认用户权限

\*\*配套操作\*\*：系统参数修改、上传规则配置、站点信息维护、全局开关调整



**富文本分片模型**（这是帖子系统的核心，照抄原生 `contents\[].contents\[]`）：

```jsonc
\[ // post.rich\_content 示例
  { "type": 1, "text": "帮我写一个早睡文案" },
  { "type": 2, "at\_user": { "id": "…", "nick": "南风来杏" } },   // @
  { "type": 3, "url": "https://…", "display\_text": "链接标题", "url\_type": 10 },
  { "type": 4, "emoji": { "id": "341", "pattern\_id": "337" } },
  { "type": 8, "topic": { "topic\_id": "12513", "topic\_name": "#腾讯会议" } }
]
```

**关键权限点**（照抄腾讯频道治理层）：发帖/评论权限、置顶、精华、删帖、禁言、踢人、身份组管理、超级管理员、内容巡检。用 `role.perms(JSON)` + 中间件统一校验，答辩时能清晰讲"RBAC"。

\---

## 四、AI 功能落地方案（DeepSeek 主 + 可切换）

统一封装 `ai/llm\_gateway`（OpenAI 兼容协议，Base URL 指向 DeepSeek；留接口可切 Qwen/GLM/通义，答辩"多模型接入"加分）。按性价比和 2G 服务器排优先级：

|优先级|AI 功能|实现要点|对应腾讯频道现象|
|-|-|-|-|
|★★★|**发帖 AI 帮写/润色**|前端"AI 帮写"按钮 → LLM 按频道风格生成/润色草稿；流式输出（SSE）|生态里的 AI 帮写（推测项），课设好演示|
|★★★|**内容巡检 + AI 审核**|① 本地敏感词表即时拦截；② 发帖异步进队列，LLM 语义审核（涉政/涉黄/广告），命中转人工或自动下架|腾讯频道 Skill「内容巡检」已上线（公开确认）|
|★★★|**社区问答机器人（RAG）**|把频道帖子做切分→embedding 存库（pgvector 或 MySQL 向量列）→提问时召回 TopK→DeepSeek 生成带引用回答；支持"AI 自动回复"开关|腾讯频道 Skill「问答自动回复」（公开确认）；原生字段还有 aiSearchInfo/highlightWords（AI 检索）|
|★★☆|**帖子/频道 AI 摘要**|摘要 Feed 卡、通知文案、频道主"运营周报"（统计+LLM 生成）|生态 AI 总结（推测项）|
|★★☆|**智能搜索**|关键词搜索（MySQL FULLTEXT/ngram） + 可选 embedding 语义召回，返回高亮词（对应原生 highlightWords）|原生 search\_guild\_feeds 返回 highlightWords + aiSearchInfo|
|★☆☆|**AI 角色"频道助手"**|一个 `member.type='ai'` 的虚拟成员（对应原生 uint32Type=2「频道助手」），可被 @ 触发问答|原生成员字段就有 AI 成员类型|
|★☆☆|**AI 绘画入口**|接一个绘图 API（或国产绘画接口），发帖编辑器支持"文生图"|Midjourney 中文版入驻 QQ（公开确认）|

**架构提醒**：AI 调用是**外部 API**（不占本地算力），2G 服务器完全扛得住；只要把 LLM 调用做成**异步任务队列**（Redis 队列 + 1 个 worker 进程），避免同步阻塞请求线程。embedding 若用 API（如 bge-m3 / 通义 embedding），本地零推理成本。**升级 4C4G** 主要是为了同时跑 WebSocket + worker + 数据库更从容，若流量小 2G 也能撑。

\---

## 五、Web + Android 双端分工

**Web（Vue3，移动优先）**：H5 也能在手机上打开（顺手满足"网页端访问"）；PC 管理后台（频道主用）：成员管理、禁言/踢人、删帖、置顶/精华、数据看板（ECharts）。
**Android（Kotlin+Compose）**：独立 App 体验——频道列表、帖子流、发帖（富文本/图片/AI 帮写）、评论、点赞、@、搜索、通知（WS 实时角标）、个人中心。用 `https://你的域名/api` + WebSocket `wss://你的域名/ws`。

> 双端共用 API 契约（OpenAPI 自动生成 SDK 或手写接口文档），这是课设"全栈"的加分叙事。

\---

## 六、实施路线（建议 8\~10 周）

|阶段|内容|验收点|
|-|-|-|
|第 1\~2 周|环境 + 数据模型 + 账号系统（注册/登录/JWT）+ 频道/版块 CRUD|能建频道、加版块|
|第 3\~4 周|发帖（富文本分片 + 图片上传）+ 评论/楼中楼 + 点赞 + 关注/加入 + 帖子流|双端能浏览、发布、互动|
|第 5 周|权限（身份组/管理员）+ 管理动作（删帖/禁言/踢人/置顶/精华）+ 搜索|管理后台可用|
|第 6 周|通知（WS 实时）+ 分享短链 + Feed 排序（热度/最新）|通知与分享闭环|
|第 7 周|**AI 一期**：AI 帮写 + 内容巡检/审核队列 + 问答机器人(RAG)|3 个 AI 功能可演示|
|第 8 周|AI 二期 + 数据看板 + 性能/安全加固（限流、SQL 防注入、越权校验）|答辩演示稿|
|第 9\~10 周|部署上线（域名/HTTPS/Docker Compose）+ 写文档/答辩 PPT|课设交付|

\---

## 七、服务器资源规划（2C2G → 可升 4C4G）

* **2G 阶段**：nginx + uvicorn(1\~2 worker) + mysql(小 buffer pool) + redis + ai-worker(1)。MySQL 关键参数：`innodb\_buffer\_pool\_size=256M`、`max\_connections=100`；静态资源走 nginx。
* **4G 阶段**：加 worker 数、加大 buffer pool、可加向量检索（pgvector/MySQL 向量）做更大 RAG。
* 建议配**域名 + HTTPS（免费证书）**，Web 与 App 都走 HTTPS/WSS，答辩更专业。

\---

## 八、给答辩准备的 3 句话叙事

1. **对标**：参照腾讯频道（QQ 内置兴趣社区，"中国版 Discord"）的产品结构与数据模型，自建频道→版块→帖子→评论/点赞→身份组→治理的完整闭环。
2. **技术**：单体 FastAPI + MySQL/Redis + WebSocket，双端（Vue3 Web + Kotlin Compose Android）共用一套 API；权限走 RBAC；Feed 走"拉取 + 缓存 + 排序"。
3. **AI**：通过统一 LLM 网关接入 DeepSeek，落地 AI 帮写、内容巡检/AI 审核、RAG 社区问答机器人（对标腾讯频道已上线的 AI 内容巡检/问答自动回复能力）。

\---

## 九、待你确认的 3 个决策点

1. **后端语言**：主推 Python FastAPI（AI 最顺）｜ 你更熟 Java Spring Boot 或 Node NestJS 也可以，方案只换后端一层。
2. **数据库**：主推 MySQL 8（答辩稳）｜ 想上语义搜索/向量可直接用 PostgreSQL 16。
3. **AI 优先做哪 2\~3 个**：建议"AI 帮写 + 内容巡检/AI 审核 + 问答机器人"，是否要加 AI 绘画/运营周报？

