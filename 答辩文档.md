# 课设答辩文档 —— dbs-curdeg（仿腾讯频道）

> 一句话：**对标腾讯频道产品结构，单体 FastAPI + MySQL/Redis + WebSocket 一套 API 双端复用（Web H5 + Android），RBAC 权限体系 + 统一 LLM 网关落地帮写/审核/问答。**

---

## 一、项目简介

| 项 | 内容 |
|---|---|
| 项目名 | dbs-curdeg（SDUdiscord）—— 仿「腾讯频道」私域社区平台 |
| 形态 | Web（移动优先 H5 + PC 管理）+ Android App，共用一套 API |
| 线上地址 | https://guild.weaxi.cn （HTTPS，Let's Encrypt 自动续期） |
| 代码仓库 | github.com/Dc-D666/dbs-curdeg（私有，push 即自动部署上线） |
| 规模 | 后端 60+ 端点、23 张表、10 个业务模块；128 个自动化测试全绿 |

## 二、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | Python 3.12 + FastAPI + SQLAlchemy 2 + Alembic | 单体模块化，原生 WebSocket |
| 存储 | MySQL 5.7（FULLTEXT ngram 中文索引）+ Redis 7 | 计数/缓存/队列/限流 |
| AI | 智谱 GLM-4.7-Flash 主 + DeepSeek 兜底（openai SDK 统一网关） | GLM Embedding-3 做 RAG |
| Web 前端 | Vue3 + Vite + TS + TDesign 组件库 | 移动优先 H5 + 管理后台 |
| Android | Kotlin + Jetpack Compose + Retrofit + DataStore | 单 Activity + Compose Navigation |
| 部署 | Docker Compose（web/api/mysql/redis）+ GitHub Actions 冒烟 + 服务器 cron 自动部署 | 健康检查失败自动回滚 |

## 三、系统架构

```
浏览器 / Android App
        │  HTTPS (REST + WebSocket)
        ▼
nginx（静态前端 + 反代 /api /ws /s/ /uploads + HTTPS 证书）
        │
        ▼
FastAPI 单体（app/api/v1 12 个路由模块 + app/services 业务层 + app/ai AI 模块）
        ├── MySQL 5.7（用户/频道/帖子/评论/权限/审核/统计…23 表）
        └── Redis 7（验证码/热度 ZSET/短链计数/审核队列/限流）
                └── AI：GLM 主 + DeepSeek 兜底（帮写/审核/问答/RAG embedding）
```

## 四、核心功能与亮点（答辩主线）

### 1. 账号与社区闭环
注册（邮箱验证码）/登录（JWT access+refresh 双 token）/资料；频道→版块→帖子→评论楼中楼→点赞→关注；图片上传；富文本编辑器（@提及/话题/emoji/行内样式/markdown 快捷语法，4.4 分片协议前后端共用）。

### 2. RBAC 权限体系（阶段 4，重点）
- 身份组 → 权限点模型，**排序即权重**：`roles.sort` 越小越靠前，可管理排序在后的身份组
- 四级默认组：频道主 / 超级管理员 / 普通管理员 / 成员（owner 与系统管理员恒全量）
- 所有管理动作（置顶/加精/删帖/禁言/踢出/拉黑/审核加入）走权限点 + **op_logs 全量留痕**
- 活跃等级：发帖+5 / 评论+2 / 点赞+1，等级身份达标自动授予、掉级回收

### 3. 实时通知（阶段 5）
- WebSocket 协议：首帧 auth（10s 超时）、30s 心跳、断线自动重连
- 事件：@提及/点赞/评论/关注/审核结果/系统通知 → **落库 + 在线实时推送**
- 通知开关（7 类可配置）、未读角标、通知中心

### 4. 分享短链 + Feed 热度（阶段 5）
- 短链：8 位随机 code、`/s/{code}` 302 跳转、Redis 同 IP 防刷计数、过期每日清理
- 热度：`score = like×1 + comment×2 + favorite×3 + 置顶权重` × 指数时间衰减，
  每频道可配置权重（feed_strategies），Redis ZSET 缓存 + 互动时增量更新

### 5. 中文搜索（阶段 4 + 7）
FULLTEXT ngram 索引（生产）+ LIKE 兜底双路召回 → 阶段 7 融合 **embedding 语义召回**（余弦补漏）；
关键词高亮 `<em>`、热门词 TOP10、搜索记录。

### 6. AI 一期（阶段 6，答辩亮点）
- **统一 LLM 网关**：openai SDK 兼容，GLM-4.7-Flash 主 + DeepSeek 自动兜底（主模型异常切换）
- **AI 帮写**：发帖页「起标题/写正文/润色」，SSE 流式打字机，一键插入编辑器
- **内容审核**：发帖异步入队（Redis）→ AI 快审（通过/驳回二态）→ 驳回自动下架 + 通知
  → **用户申诉** → 大 max_tokens 复审（通过/驳回/转人工三态）→ 管理员人工兜底
- **RAG 问答**：帖子 embedding（GLM Embedding-3）存列，余弦召回 TopK，LLM 带引用回答；
  全局问答浮窗，回答可跳转原帖

### 7. 运营看板与加固（阶段 7~8）
- 系统管理员看板：全局统计 + 近 7 天发帖趋势 + Top 频道
- 接口限流：登录 20/min、注册/验证码 10/min（Redis 固定窗口，429+3001）

### 8. 工程化
- **128 个 pytest 全绿**（事务回滚测试库、LLM 全 mock 不调外部 API、WS 端到端）
- CI 冒烟 + **push 即上线**（服务器 cron 3 分钟拉取重建，健康检查失败自动回滚到上一提交）
- 数据库迁移只用 Alembic（容器启动自动 upgrade head）

## 五、演示脚本（10 分钟版）

1. **开站**：访问 https://guild.weaxi.cn → 注册（邮箱验证码）→ 登录
2. **建频道发帖**：创建频道（自动建默认版块）→ 发一篇富文本帖子（@ 成员/话题/emoji）
3. **实时通知**：开两个浏览器，A 评论 B 的帖子 → B **不刷新**收到角标 + 推送（重点演示 WS）
4. **权限**：频道管理后台 → 身份组配置 → 给成员禁言 → 该成员发帖被拒；op_logs 留痕
5. **热度**：切换帖子流「热门」与「最新」，对比排序（权重 + 时间衰减）
6. **搜索**：中文关键词搜索 → 高亮；再问一句语义相关但不含关键词的话 → 语义召回
7. **短链**：帖子详情「分享」→ 复制短链 → 无痕窗口打开跳转
8. **AI 帮写**：发帖页点「AI 写正文」→ 打字机生成 → 插入 → 发布
9. **AI 审核**：发一条违规内容 → 自动下架 + 通知 → 点申诉 → 复审恢复
10. **问答机器人**：右下角浮窗提问 → 引用站内帖子回答
11. **看板**（super 账号）：运营看板趋势图
12. **Android**：模拟器演示登录/首页流/帖子详情（若时间允许）

## 六、可能的问答准备

**Q：为什么不用微服务？**
服务器 2C/3.5G 内存有限，单体模块化最省资源；答辩更聚焦业务本身（方案决策 D1）。

**Q：权限模型怎么设计？**
身份组存权限点集合；`sort` 排序即权重（可管理排序在后的组）；owner/系统管理员恒全量；
写路径统一 `require_perms` 依赖工厂校验，管理动作全部 op_logs 留痕可审计。

**Q：热度和短链的并发问题？**
计数全部 SQL 原子自增（`count = count + 1`，点赞唯一约束防重复）；热度缓存 Redis ZSET
惰性重建 + 互动增量更新；短链计数 Redis 同 IP 去重 + 批量落库。

**Q：AI 怎么接的？**
openai SDK 兼容协议 → 智谱 GLM 为主，异常自动切 DeepSeek；审核走 Redis 队列异步消费
（不阻塞发帖）；RAG embedding 存 MySQL JSON 列（5.7 无 VECTOR 类型，应用层余弦，课设规模足够）。

**Q：WebSocket 推送在同步端点里怎么发？**
FastAPI 启动时捕获主事件循环，业务服务用 `run_coroutine_threadsafe` 投递推送，线程安全。

**Q：部署链路？**
push → GitHub Actions 冒烟测试 → 服务器 cron 每 3 分钟拉取 → Docker 重建 →
健康检查通过才算成功，失败自动回滚上一提交（曾真实触发过并修复）。

## 七、遗留与展望

- favorites 收藏表未实现（热度公式权重位已预留）
- Android 需 Android Studio 构建验证 + WS 实时化（当前进页拉取）
- AI 二期：CogView 文生图、运营周报摘要
